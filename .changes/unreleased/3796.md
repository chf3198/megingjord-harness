### Added

- The HAMR MCP adapter (`scripts/global/hamr-mcp-adapter.js`, split into `hamr-mcp-tools.js` +
  `hamr-mcp-transport.js`) now lands on `main` **dark** behind the default-OFF
  `MEGINGJORD_MCP_ADAPTER_ENABLED` gate (#3796, Epic #3789): `main()` is fail-closed and refuses to
  serve unless the flag is `1`. Registered via `.github/copilot-mcp.json` with the flag unset
  (default-OFF), so nothing new executes on merge — the flip is a reversible, evidence-gated flag write.

### Changed

- `config/flag-lifecycle.json` advances `MEGINGJORD_MCP_ADAPTER_ENABLED` from `dark` → `canary`
  (one non-critical session class), with the #3795 canary emitter + auto-rollback armed. This
  dissolves the retained PR #3050 protected-main-merge carve-out: the mock-negative
  `ephemeral_verification_evidence` (forged-proof/replay/wrong-audience/no-key-leak — all reject) plus
  the fail-closed gate and canary make the merge reversible and non-security-weakening.
