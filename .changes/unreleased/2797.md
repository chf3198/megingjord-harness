### Added
- Cross-family generator-critic verification of fleet-authored code with prove-it escalation (#2797, Epic #2791 P1-4). New `scripts/global/fleet-cross-family-critic.js` dispatches a fleet-authored pr-diff to N different-family critics (reusing #2041 `dispatchRedTeam`), takes a majority panel verdict, and decides mergeability under the invariant that objective gates are authoritative: a critic REJECT with gates green is advisory unless a flaw reproduces (re-arm the verifier) or is an unprovable high-severity security pattern (escalate one tier) — never silent-merge. Scans every critic before honoring a majority accept (a minority critic's flaw outranks it), prove-its every REJECT keyword-independently, and exposes an injectable flaw classifier (`deps.classifyFlaw`) for the #2888 model-upgrade seam. Cross-family reviewed (gemini-2.5-pro, ACCEPT 10/10 after a 4-round loop that caught a critical silent-merge path + two high false-negatives pre-merge).
