- **Auto-mode merge authority provisioned at install (#3346):** `npm run hamr:activate` now auto-applies #3342's `automode-provision` (step 6/6, opt-out `MEGINGJORD_NO_AUTOMODE_PROVISION=1`), so a freshly-provisioned harness host carries the scoped baton merge authorization with no in-session and no client act — closing the gap where the provisioner existed but was never wired in. The install runbook now frames it as an IT-owned host-provisioning step, documents the pre-#3342 bootstrap path, and links the upstream Claude Code feedback requesting a supported non-interactive grant.
