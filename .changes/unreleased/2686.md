- Add an advisory `pre-commit` check (`scripts/global/feedback-memory-promotion-check.js`)
  that flags newly-added operator-memory `feedback_*.md` files, prompting the operator to
  consider promotion to canonical `instructions/` per Epic #2399. Non-blocking; fires only on
  new additions. (#2686, Epic #2399 AC5)
