### Added
- **Fleet-review hard-gate** (#2738, Epic #2192): `scripts/global/megalint/fleet-review-required.js` makes cross-family fleet/free-cloud review NON-skippable at Consultant phase on review-required lanes — promoting the fleet-review contract from instruction to enforcement (G1) and mandating the free review lane (G3). 3-fact anti-forgery: cross-family (reviewer family != author family), verdict format, dispatch-provenance. Registered as an `enforced` link in `config/governance-chains.yml`, so #2709's `chain-integrity.js` self-enforces it (it cannot be silently removed or weakened).
