### Added
- Objective test-floor classifier (#3098, re-ship of Epic #1948 Phase-1 P1.1+P1.2 — the original children #2652–#2657 were phantom-closed with no merged code). New `scripts/global/test-floor-classifier.js` derives the objective minimum `test_strategy` floor from the changed file set per the test-methodology matrix (reusing `stress-surface-audit` content signals) and reconciles it against the Manager-declared strategy, flagging (1) a stress-triggering surface declared without `+stress-test` (the #1682 motivating case) and (2) a below-floor declaration on real code. Run `npm run test-floor:check -- --declared <strategy> --files a.js,b.js`. Ships advisory (`--strict` for non-zero exit); promotion to a blocking gate is replay-eval-gated. Covered by `tests/test-floor-classifier.spec.js` (tdd-pyramid) + `tests/stress-test-floor-classifier.spec.js` (adversarial-path fault injection + p99 budget).
