### Added

- New governance contract `instructions/resource-tier-portability.instructions.md` (loaded via CLAUDE.md) codifies the canonical **Tier 0–5 resource taxonomy** from Phase-0 #2412: Tier 0 (local only) → Tier 1 (GitHub baseline) → Tier 2 (HAMR/Cloudflare) → Tier 3 (Tailscale fleet) → Tier 4 (paid LLM keys) → Tier 5 (enterprise), each with its opt-out/fallback signal (`MEGINGJORD_HAMR_DISABLED`, `MEGINGJORD_FLEET_DIRECT_BLOCK`, `MEGINGJORD_MCP_DISABLED`, provider cascade). It references — without duplicating — the G5 contract and tier-graceful pattern in `harness-goals.instructions.md`, and documents the forthcoming `MEGINGJORD_MINIMUM_TIER` assertion (enforcement deferred to a sibling Phase-1 child). Keystone of Epic #2398 Phase-1; downstream consumer for #2488 Phase-1 feature tagging (#2631).
