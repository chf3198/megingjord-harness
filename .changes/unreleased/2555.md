### Fixed

- Automated xteam MCP registration for Copilot, Codex, Claude Code, and Antigravity via `scripts/global/xteam-mcp-register.js`, wired into deploy scripts for apply and target-specific flows.
- Restored xteam MCP runtime startup by invoking `main()` from `scripts/xteam-mcp/bin.js` and switching prompt registration to SDK-compatible `McpServer.registerPrompt(...)` in `scripts/xteam-mcp/index.js`.
- Added regression coverage in `tests/xteam-mcp-register.spec.js` for idempotency, merge behavior, dry-run mode, and target-isolated failure handling.
- Clarified install runbook scope in `docs/howto/xteam-mcp-install.md` (`deploy:all:apply` for all runtimes, `deploy:apply` for Copilot-only registration).

Refs #2555.
