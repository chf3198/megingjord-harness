### Added
- `instructions/global-standards.instructions.md` gains a "Codified rules-of-thumb" registry section (Epic #2399 AC3, #2685): a compact table mapping each rule-of-thumb promoted from operator-local memory to its **verified** canonical instruction location plus an honest `codified | documented-elsewhere | memory-only` status marker. Runtime-agnostic (Copilot / Claude Code / Codex / Antigravity). Every cited file + section anchor is grep-verified to exist, remediating the Phase-0 audit's phantom-heading drift. Section shape chosen by unanimous free-cloud cross-model consensus (groq + mistral + sambanova).
