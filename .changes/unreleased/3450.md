- **Scaffold onboarding surfaces 15-20 (#3450, Epic #3411 T2.7):** extended the `harness:add-runtime` scaffold from 11 to 17 onboarding surfaces — added `lefthook`, `governance-profiles`, `instruction-set`, `auth-profile`, `goal-tier`, and `config-dir`, each with a plan-action builder, idempotent already-present detection, and a golden assertion. All 5 committed runtimes re-scaffold fully already-present (17/17); the T2.2 golden proof stays green. Completes the one-step onboarding fan-out. AGENTS.md evaluated as a surface-reduction lever (free-panel adjudicated): collapse per-runtime prose-instruction entry-files only; structured-enforcement surfaces (auth-profile, goal-tier, governance-profiles) stay separate JSON.
