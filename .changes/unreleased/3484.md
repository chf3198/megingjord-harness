- **Fleet keep-warm pool + stakes routing (#3484):** `cascade-dispatch` now routes routine work to a keep_alive-pinned resident 7B (no cold-load stall) and reserves the CPU-offloaded 32B for genuine high-stakes work via a deterministic stakes gate (`scripts/global/fleet-stakes-router.js`) that composes with `per_role_lane_preferences`. `keep_alive` is threaded through the LiteLLM + direct-Ollama request bodies, and `scripts/global/fleet-warm-pool.js` warms the hot model on session start. Directly addresses the Epic #3414 cold-load + 32B-on-common-path penalties (the F2 worked example).
