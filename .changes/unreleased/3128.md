### Added
- `npm run memory:route` (`scripts/global/memory-write-router.js`, #3128): a pure, deterministic `route(factClass)` mapping each fact-class to its single canonical home per the Epic #3124 D4 table (rule→instructions, durable-pattern→wiki/wisdom/project, operator-pref→~/.claude private, incident→incidents.jsonl, …), so durable facts stop scattering across surfaces.
- `npm run memory:dedup` (`scripts/global/memory-dedup-lint.js`, #3128): an advisory lint that flags a `pattern_id`/normalized-title appearing in 2+ canonical surfaces (deterministic, no embeddings); `--strict` opts into a non-zero exit.

### Changed
- `docs/workflow/learnings.md` is now a thin chronological pointer index; the full prose moved to its canonical home `wiki/wisdom/project/research/workflow-learnings.md` (Epic #3124 D4 — one fact, one home, no duplicated prose).
