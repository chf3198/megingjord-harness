### Fixed
- The doc-coverage Tech-Writer gate and the cross-family review check are now actually enforced on `lane:code-change` PRs. They were dead code: `collaborator-handoff.js` gated both on an `input.lane` scalar that the CI workflow never passed (it passes `labels`), so the blocking-capable validator shipped in #2712 never fired. The gate now derives the lane from labels, parses both `doc-coverage:` and `doc_coverage:` block keys, fails **closed** on a matrix-load error, and honors a `LEGACY_DOC_SKIP` escape hatch only when a `BLOCKER_NOTE` is present on the linked issue. (#3016, Epic #2707)
