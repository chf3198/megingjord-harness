- **Verify-don't-trust worktree freshness (#3858, Epic #3854 GAP-B):** the collaborator worktree
  freshness gate now RECOMPUTES `behind` (fail-open `git fetch` before count via
  `git-freshness-check.behindCountFresh`) instead of trusting the declared `worktree_behind_main`.
  `worktree-lifecycle-gate.checkCollaborator` emits `worktree-behind-declared-mismatch` when
  declared ≠ recomputed, and `worktree-behind-exceeds-tier` only above the existing velocity-relative
  tier ceiling (30) — never a bare `>0` (Epic #1771/#1827 absolute-threshold ban). Recompute is
  fail-open (offline / detached CI checkout → skip).
