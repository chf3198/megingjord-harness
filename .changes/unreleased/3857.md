- **PreToolUse worktree-add guard (#3857, Epic #3854 GAP-A/GAP-E):** `git worktree add` is now
  intercepted before it runs — denied when based on a stale `origin/*` (no `git fetch` within
  300 s → redirect to `scripts/agent-worktree.sh`, G8 event), when a `-b` branch name violates
  naming, or when a non-branch base would create a detached HEAD. Fail-open on any error.
