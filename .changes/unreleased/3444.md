- **One-step runtime onboarding scaffold (#3444, Epic #3411 T2.1):** `npm run harness:add-runtime -- --runtime <id>` generates a deterministic plan of every per-runtime onboarding artifact (11 surfaces: descriptor, actor-map, runtimeKinds, VALID_TEAMS, detect-runtime, signatures, self-test registry, governance-parity, deploy.sh, npm deploy scripts) derived from the feature catalog + descriptor — collapsing the ~15-20-file hand-edit fan-out to one command. Dry-run by default; `--apply` writes transactionally with full rollback on partial failure; `HARNESS_SCAFFOLD_SEED` determinism; idempotent (`already-present` no-ops). Re-scaffolding cursor reports 0 pending / 11 already-present, proving fidelity to the real wiring.
