- **Golden re-scaffold acceptance test (#3445, Epic #3411 T2.2):** `tests/harness-add-runtime-golden.spec.js` + `tests/helpers/golden-normalize.js` re-run the T2.1 scaffold for all 5 committed runtimes and assert every action is `already-present` — the content-hash-equivalence proof that the generator faithfully reproduces each runtime (normalizer strips volatile fields; `tests/fixtures/harness-rescaffold-golden.json` pins per-runtime hashes). Surfaced and fixed a real scaffold fidelity gap: the plan builder now resolves committed deploy aliases (claude-code→`deploy:claude`, copilot→`deploy:apply`) via a documented DEPLOY_ALIAS map so existing runtimes re-scaffold as a no-op while new runtimes still get `deploy:<id>` create actions.
