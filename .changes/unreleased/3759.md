- **Wiki reconcile liveness SLO — silent freeze is now impossible (#3759, Epic #3719).** A new scheduled
  `wiki-reconcile-liveness` monitor enforces "≥1 successful `wiki-reconcile-cron` run per 24h" by reading the
  Actions run-history (a durable, non-gitignored source of truth — the #3718 alarm self-silenced by writing to
  gitignored state), files a deduped durable GitHub-issue alert on breach, and **hard-fails** (a required signal
  with no precision/corpus dependency). The deduped `if: failure()` durable alert is also generalized to
  `wiki-work-log-mirror`. `scripts/wiki/reconcile-liveness.js#checkLiveness` is pure + unit-tested.
