- **Governance — unforgeable signer-independence (Epic #3679 F2+F3, #3672):** the Admin-gate
  independence check now fails-closed on manufactured/forged evidence. A same-team persona/role-surname
  split and a self-asserted `signer-independence-check:` disposition that is *waived / N/A* are rejected
  distinctly (`independence-self-waived`); a **bare "different Team&Model team" claim no longer
  auto-passes** — it is forgeable (a single agent can mint a foreign-team signer), so independence now
  requires a **verified cross-family consensus receipt** or a cryptographic authorship attestation, for
  same- and different-team alike (`unattested-cross-team-claim`). The three surfaces —
  `baton-independence.checkAdminIndependence`, `baton-authority/evidence-loader` (the merge FSM), and
  the `consensus-receipt-check` CI validator — are kept consistent. Also fixes a latent gap where
  `findRoleComment` did not match the real `## ADMIN_HANDOFF` markdown header, so `checkAdminIndependence`
  silently skipped on every real handoff. The attestation mechanism is fail-closed pending research #3682.
