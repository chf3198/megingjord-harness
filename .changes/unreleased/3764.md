- **Human browse/search + curation on the wiki dashboard reader (#3764, Epic #3719).** The dashboard wiki
  reader (previously wisdom-global-only, no search/curation) gains `browseWiki` / `searchWiki` / `curatePage`
  (`scripts/wiki/dashboard-reader.js`, wired into `scripts/dashboard-wiki.js`): browse + search across A=code,
  B=work-log, C=wisdom over both the global and workspace scopes (search backed by the shipped lexical
  retrieval floor), plus a curation action (edit / flag-stale) that routes **only** through the validated
  `wiki-io#writePage` path — applying credential-class secret redaction (#3772) and frontmatter/index
  validation (#3763), with no schema bypass. The existing `getWikiPages` is preserved. Cross-refs #2508 (not
  closed).
