### Added
- Least-Agency protected-path deny-list for autonomous fleet-development (#2798, Epic #2791 P1-5; OWASP-2026 Least Agency, design D9). New `scripts/global/fleet-dev-deny-paths.js` is a hard pre-filter (`screenFleetDev`) that runs before the #2794 capability gate: high-blast-radius paths (auth, crypto/key-management, IAM/permissions, governance gates + `pretool_guard`, CI/release pipelines, listed in `model-routing-policy.json: fleet_dev_deny_paths`) are off-limits to fleet-authored code regardless of measured capability — only premium/human may author. Fail-closed on a missing/empty deny-list, undeclared paths, or any path that cannot be safely canonicalized; deny decisions are logged. `normalizePath` is hardened against a 7-vector evasion battery (URL/multi-encoding, Unicode homoglyph, path traversal, Windows trailing dot/space, segment-erasure, null-byte/control-char, glob-metacharacter, and argument injection) — cross-family reviewed (gemini-2.5-pro, ACCEPT 10/10 after a 7-round adversarial loop).
