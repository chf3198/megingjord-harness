### Fixed
- `scripts/global/lint-epic-drift.js` now recognizes Epic-level `## CONSULTANT_EPIC_CLOSEOUT` artifact (per `instructions/epic-governance.instructions.md` "Epic Progress Comment Protocol") alongside per-child `## CONSULTANT_CLOSEOUT` and `## Epic Progress Update` patterns. Previously, atomic-close-all-children Epic patterns emitted false-positive Class C drift findings, blocking pre-push gates after Epic close cycles. Extracts `childProgressComplete(child, comments)` pure predicate for unit testability. 8 regression tests cover the EPIC_CLOSEOUT match, multi-child enumeration, and substring-match limitation documentation. Tier-2 anneal close. Refs #1993.
