### Added

- `docs/howto/state-isolation-migration.md` (Epic #2091 C9): operator migration guide for the state-isolation fix-set (per-session state keying, session-boundary hooks, per-worktree `core.hooksPath`, canonical-main read-only enforcer, audit log) — what changed, what operators must do (work in a worktree; bootstrap pre-existing ones), how to verify, and the #2647 scope boundary.
