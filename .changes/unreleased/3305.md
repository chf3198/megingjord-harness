- **Fleet availability health signal** (#3305): a fleet *outage* (Ollama probed unreachable while fleet-eligible work was attempted and failed over) is now distinguished from *no-demand* and emitted as a loud, observable signal (`incidents.jsonl` pattern `fleet-availability-outage` + `dashboard/events.jsonl` v3), so silent fleet underutilization can no longer masquerade as ordinary low utilization. Rollback: `FLEET_HEALTH_SIGNAL_DISABLED=1`.
