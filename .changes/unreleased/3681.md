### Fixed

- **Merge-bypass detection (Epic #3679):** `admin-merge-exception` now iterates over the
  *required* contexts instead of the runs present, so a required check with no green run
  (missing / pending / never reported) counts as NOT green — a genuine `--admin` bypass on a
  never-green required check is now detected and fails the gate instead of reporting PASS. (#3681)
