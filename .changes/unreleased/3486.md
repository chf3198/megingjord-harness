- **Fleet Advisor F3 host selector + Host-B repair runbook (#3486):** `scripts/global/fleet-host-selector.js` picks the least-loaded warm host across a mesh (resident-model count + free VRAM from `/api/ps`), degrading safely to a single host (F2) when a peer is down and to the free-cloud third tier when all are down. `docs/howto/host-b-repair-runbook.md` gives the IT-role triage ladder (Tailscale → power → Ollama bind → firewall → verify) to restore a downed second host. Part of the Fleet Advisor capability (Epic #3414, §Q3).
