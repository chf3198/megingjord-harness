- **Autonomous stuck-state resolution (#3748):** Added `scripts/global/stuck-state-detector.js` — the unified detector that auto-routes a stuck-PR / ambiguous-gate / novel-failure into the cross-model `adjudication-guardrail.decide()` panel instead of deferring to the client. Fires on loop-fingerprint (reusing `friction-sensors`), iteration/token-budget caps, tool-error bursts, cross-family self-consistency divergence, or an explicit stuck signal; escalate-vs-resolve is gated on reversibility/blast-radius (never self-reported confidence), with hysteresis to prevent flapping. Ships **advisory**; `stuck-state-replay-eval.js` + `tests/fixtures/stuck-state-corpus.json` bound the false-positive rate and gate any advisory→blocking promotion at precision ≥0.85 (never calendar). Live hook activation deferred to #3766.
