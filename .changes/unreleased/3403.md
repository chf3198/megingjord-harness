- **Adjudicate-first for the 2 security-sensitive hook surfaces** (#3403, Epic #3392 AC2/AC5): the S6 hook-script-mutation and S7 sensitive-file-path surfaces in `pretool_guard.py` are now precise instead of blanket-prompting. A fast in-process risk classifier (mirroring the #3401 adjudication-guardrail risk-tiering — no synchronous network panel in the hot path, G7) lets benign access proceed: S6 allows a hook **read/inspect** and a **governed deploy** that writes hooks, S7 allows reading/sourcing a **gitignored-local** secret file (the operator's own `.env`). The human carve-out is reached ONLY on genuine risk — a direct ungoverned hook **mutation** (policy-weakening) or a **tracked/committable** secret-file path (exposure). Both classifiers are **fail-closed** (any error → ask), so a bug can never silently weaken G4. No security control was removed.
