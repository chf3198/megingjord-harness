### Fixed

- **Baton hardening (F1, Epic #3679):** the COLLABORATOR_HANDOFF gate now **ledger-verifies** a cited
  `cross_family_receipt` instead of accepting any 16-hex string. A fabricated-but-well-formed receipt
  fails closed at handoff-emission time (`cross-family-receipt-unledgered`), not only at the merge gate —
  closing the root shift-left hole from #3673 / PR #3677. Enforced at both the local self-check
  (`collaborator-handoff-schema.js`) and the server gate (`megalint/collaborator-handoff.js`) via one
  shared rule; a tampered ledger chain is rejected (`cross-family-receipt-ledger-tampered`). Full
  ≥2-family / unanimous-PASS verification remains at the merge gate (defense-in-depth). (#3678)
