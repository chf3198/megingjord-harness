### Added
- Review-context anti-bypass intercept (#2933, Epic #2926 C7): the #2739 raw-fleet-curl intercept now also flags **raw cross-family-provider HTTPS** (gemini/openai) issued in a **review context**, keeping the $0 cascade the default review path. `hooks/scripts/hamr_fleet_direct_block.py` + its JS twin `scripts/global/hamr-fleet-direct-block.js` gain `is_review_context`/`review_bypass_decision`; `pretool_guard.py` surfaces an **advisory** (soak phase) and **denies** when `MEGINGJORD_REVIEW_BYPASS_BLOCK=1`. Detection only acts on `paid-bypass` severity (fleet-bypass stays the existing block's job) and honors suppression overrides. The bypass is counted via `emit_incident`, so the raw-bypass→0 metric is measurable. D7; G1 governance.
