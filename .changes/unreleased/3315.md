- **fix(governance): non-bricking baton-authority ruleset + live merge enforcement (#3315)** — Corrected `scripts/global/baton-authority/ruleset-config.js` so the committed config no longer requires `baton-fsm-conformance`, a context that is never reported (its job is `fsm-conformance-advisory`, `continue-on-error` + paths-filtered) and would have left every PR stuck "Expected — waiting for status". The only required check is now `baton-authority/merge` (real, unconditional, always reports). Removed the placeholder break-glass actor (`actor_id: 0`); `bypass_actors` is now empty with active admin enforcement, with break-glass handled at the workflow level via the `merge-bypass:admin-exception` label. Added a `KNOWN_REPORTING_CONTEXTS` validator invariant that rejects any required context no workflow reports (anti-recurrence). `apply-ruleset.js --apply` now performs a real idempotent POST/PUT-by-name against the live repo. Refs #3284, #3290, #3288.
- **fix(governance): rebrand-proof ruleset apply (#3315)** — `apply-ruleset.js` now resolves the live repo slug at runtime via `gh repo view` (fallback to the canonical `chf3198/megingjord-harness`) so the live `--apply` cannot 307 after a repo rename — the rebrand from `devenv-ops` had left a stale `REPO` constant that GitHub redirects on GET but not POST.
- **fix(governance): API-valid ruleset payload (#3315)** — `buildRulesetConfig` now omits `integration_id` from required status checks; the GitHub Rulesets API rejects `integration_id: null` (HTTP 422). With this, the live ruleset is applied and enforces `baton-authority/merge` on `main`.
- **fix(governance): deferred-final merge from `status:review` no longer bricks (#3315)** — The live, required `baton-authority/merge` ruleset gate previously denied every deferred-final PR (`fsm-denied: illegal-transition`) because the FSM (`scripts/global/baton-fsm/transitions.js`) modelled `MERGE` only from `status:testing`, while the documented deferred-final flow merges from `status:review`. Added a `REVIEW + MERGE` transition (requires `CONSULTANT_CLOSEOUT | CI_GREEN | SIGNER_INDEPENDENT | WORKTREE_MERGE_OK`; self-loop — terminal close stays the separate `REVIEW + CONSULTANT_CLOSEOUT → DONE` row), rebuilt `kernel.wasm`, and regenerated the W-method conformance corpus. Real casualty that confirmed the brick before the fix: PR #3324 (#3300). Refs #3284, #3290, #3287.
