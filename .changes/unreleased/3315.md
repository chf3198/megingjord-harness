- **fix(governance): non-bricking baton-authority ruleset + live merge enforcement (#3315)** — Corrected `scripts/global/baton-authority/ruleset-config.js` so the committed config no longer requires `baton-fsm-conformance`, a context that is never reported (its job is `fsm-conformance-advisory`, `continue-on-error` + paths-filtered) and would have left every PR stuck "Expected — waiting for status". The only required check is now `baton-authority/merge` (real, unconditional, always reports). Removed the placeholder break-glass actor (`actor_id: 0`); `bypass_actors` is now empty with active admin enforcement, with break-glass handled at the workflow level via the `merge-bypass:admin-exception` label. Added a `KNOWN_REPORTING_CONTEXTS` validator invariant that rejects any required context no workflow reports (anti-recurrence). `apply-ruleset.js --apply` now performs a real idempotent POST/PUT-by-name against the live repo. Refs #3284, #3290, #3288.
