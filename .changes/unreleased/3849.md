### P1-c: per-repo override contract docs (Epic #2892)

Documents the per-repo guardrail-override contract shipped in P1-a/P1-b: a full operator guide
`docs/howto/repo-overrides.md` (the 3-tier taxonomy, the hard-floor list, how to declare
`.megingjord/overrides.yml`, and the G8 audit) plus a concise "Per-repo guardrail overrides"
reference in `instructions/global-standards.instructions.md`. Documented in an existing instruction
(not a new always-resident file) → net-neutral on resident governance surface.
