- **Research-redteam corpus-overlap stage (#2801):** `/research-redteam-loop` now runs a mandatory deterministic corpus-overlap scan (`scripts/global/corpus-overlap-scan.js`) over the design scope before ACCEPT. It reuses the #2617 tokenizer but scans **open and closed** issues (the near-miss #2518 was a closed epic) using an overlap-coefficient metric, emits a `related_tickets:`/`overlap_decision:` block, feeds candidates to the cross-family reviewer, and blocks ACCEPT when overlap exists with no recorded boundary decision. Distinct from the #2617/Epic #2623 ticket-creation gate (design-consensus lifecycle stage).
