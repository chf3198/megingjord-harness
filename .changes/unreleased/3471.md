Made the canonical-main read-only enforcer quote/here-doc-aware so a comparison or arrow operator in prose (goal-lens `G1 > G2`, `aggregate >= 0.85`, `> and >=`) inside a quoted string, a here-doc body, or a tool description field is no longer misparsed as a shell output redirect and false-blocked. Genuine redirects to tracked main files are still denied. (#3471)
