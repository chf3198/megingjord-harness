### Tests

- Add AC5 regression spec for the Dashboard Fleet Setup API + wizard (`tests/dashboard-fleet-setup-3173.spec.js`) — 10 node:test assertions covering the status route, server-side credential write (keychain/atomic `.env`, never localStorage), absence of API-key fields in `fleet-settings.js`, wizard wiring, and route guards (404/405). (#3173)
