- feat(hooks): add a structured per-team friction-event schema + emitFriction helper (JS `scripts/global/friction-event.js` + Python `hooks/scripts/friction_event.py`) that writes redacted schema-v3 `tier:1` events keyed by a stable `pattern_id` to `~/.megingjord/incidents.jsonl`, turning ad-hoc friction prose into standing anneal feedstock; the existing anneal-tier2-autofile recurrence model now routes recurring (≥2-in-7d, cross-team weighted) frictions to Tier-2, with a seed catalog of confirmed pattern_ids and advisory-first rollout (#3165)
