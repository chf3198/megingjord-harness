### Added

- New governance contract `instructions/cross-team-communication-tiers.instructions.md` (loaded via CLAUDE.md) defines the 3-tier cross-team communication model and which coordination substrate is the default per tier: **Tier-1 (single workspace) → GitHub-native** (Issues/labels/refs/Releases/Actions, zero Cloudflare), **Tier-2 (cross-workspace) / Tier-3 (cross-user) → HAMR**. Includes the `MEGINGJORD_HAMR_ENABLED` selector and Tier-1 fallback ordering. Closes Epic #2488 Phase-0 (with #2489), unblocking the zero-Cloudflare Phase-1 implementation (#2628).
