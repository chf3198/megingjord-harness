feat(governance): replay-eval-gated promotion for the backlog-drift guardrail (#3423, Epic #3398 C5)

Ships a labeled corpus (tests/fixtures/backlog-drift-corpus.jsonl, seeded from #1899 TP + true-negatives) and a $0 deterministic replay-eval (scripts/global/backlog-drift-replay-eval.js) that scores supersession precision + false-supersede-rate and reports promotionEligible. The advisory→blocking flip is gated on precision ≥ 0.85 AND false-supersede-rate ≤ 0.05 — replay-eval-calibrated, never calendar-gated (counsel R4: a wrong cancel is the costly error). Human review is required for any flagged P1/Epic cancel until the margin is met; conformal-style uncertainty demotes low-confidence verdicts to advisory.
