fix(governance): re-land Epic #1271 AC-reconciler deliverable (closed-without-merge casualty)

Re-lands the per-AC truth-status reconciler (`scripts/global/epic-ac-reconcile.js`),
its EPIC_RESCOPE parser dependency, the `declared_complete_unmet_ac` Goal-Health-Score
sensor (`sensors/dc.js`), the manager-narrative advisory linter, and the two CI workflows
(`epic-ac-reconcile.yml`, `manager-narrative-lint.yml`) onto `main` in REPORT-ONLY mode.
Originally closed `resolution:completed` on Epic #1271/#1289 but never merged; this provides
the validation backstop Epic #1299 depends on. Disposition (RE-LAND, not superseded) signed
off by a unanimous 4-model free-cloud cross-model panel. Refs #3319, #1271, #1289, #1889.
