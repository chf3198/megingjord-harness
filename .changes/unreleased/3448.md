- **Antigravity enforcement-plane wiring (#3448, Epic #3411 T2.5):** closed the Antigravity enforcement gaps and proved the gates FIRE. New `scripts/global/antigravity-hooks-emit.js` deterministically emits `.antigravity/hooks.json` wiring the 9 required hook scripts to the correct camelCase SDK events (was mis-wired with invalid event names); added the missing `~/.antigravity/hamr-config.json` to `hamr-provider-wrapper.js` `TEAM_CONFIG_PATHS` (five-runtime HAMR parity); expanded the antigravity descriptor's events + `emitScript` and cleared its `hook-wiring` waiver. `tests/antigravity-enforcement-wiring.spec.js` (20 tests) invokes the Python hooks via subprocess with synthetic events and asserts a real enforcement decision — a fires-check, not file-presence.
