- **Self-governance replay-eval external judge (#3827, Epic #3822 C3):** a committed replay-eval
  (`scripts/global/self-governance-replay-eval.js`) scores the two enforced self-governance
  interceptors — the ask-time reference monitor (#3825) and the plan-rating promotion gate (#3826) —
  against a labeled corpus by driving the REAL shipped artifacts, and reports catch-rate /
  false-escalation / carve-out-recall plus a **Gwet AC1** chance-corrected calibration of the ≥90
  threshold. Wired as a REQUIRED CI gate (`self-governance-replay-eval.yml`); promotion is
  replay-eval-gated, not calendar-gated. The carry-forward empirical run surfaced and closed two
  G4-relevant under-catches in the ask-monitor (rotate-credential / broaden-permissions).
