fix(scripts): watcher-health now updates one triage ticket per (repo, workflow) instead of filing a duplicate `[WATCHER-HEALTH]` issue every 6h run — the dedupe match is now a deterministic client-side marker check rather than an operator-laden `gh --search` string (#3650).
