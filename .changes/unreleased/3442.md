- **Enum/registry parity reconciliation (#3442, Epic #3411 T1.4):** brought six drift-prone runtime registries to full 5-runtime parity — L1 actor-map (codex, antigravity), `runtimeKinds` (cursor), `leader-election.js` VALID_TEAMS (cursor), governance-manifest `targets` enum (codex, cursor), governance-rules `cross_runtime_applicability` (cursor on 8 rules), and self-test `adapter_exemptions` (cursor, antigravity). Aligned `runtime-side-effect-guard.js` ALLOWLIST and the `orchestrator-compatibility.spec.js` KNOWN array. Golden fixture `tests/fixtures/enum-parity-3442.json` anchors the membership against regression. Documented structural-N/A: copilot (github-actions runner, no local commandId surface).
