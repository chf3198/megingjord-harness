### Added
- Anti-Goodhart test-quality monitor + critic-calibration meta-guardrails for the fleet-dev control loop (#2799, Epic #2791 P1-6; design D10/D11). `scripts/global/fleet-dev-anti-goodhart.js` captures per-change quality samples (new-code coverage delta + mutation score + test/code complexity ratio) and, over a velocity-relative window using a least-squares slope (so slow consistent declines aren't missed), trips when the fleet-development-share metric rises while composite quality falls — emitting a Tier-2 anneal and gating the share metric so G3 cannot silently erode G2. `scripts/global/fleet-dev-critic-calibration.js` tracks the #2797 critic's precision vs human-validated escalations, the prove-it test-acceptance rate, and post-refresh verdict drift; each check reports `ok`/`degraded`/`insufficient-data` (a starved metric pipeline surfaces as unknown, never as silent-healthy), tripping an anneal + recalibrate signal on degradation. Cross-family reviewed (gemini-2.5-pro, ACCEPT 10/10 after a 3-round loop that caught a slow-drift blindspot and a fail-open data-starvation path).
