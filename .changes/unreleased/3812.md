fix(governance): resident-load budget + fail-closed on-demand migration (#3812)

Epic #3807 C4. Migrates `resource-tier-portability.instructions.md` (a G5 tier *taxonomy*, whose
binding partner `harness-goals` is already on-demand) out of the always-resident `@`-import set into
the on-demand section — the rule is kept, only its always-on load is retired (resident LOC −68,
`surface_units` −1, net-negative). Adds `scripts/global/resident-load-budget.js` (READ-ONLY, tier-1):
a resident-LOC budget check against the committed census baseline, plus a **fail-closed** on-demand
loader — a migrated rule that cannot load when its operation needs it throws `OnDemandLoadError` so
the dependent operation BLOCKS/warns rather than proceeding rule-less (no silent rule-loss). No G1/G4
control weakened (cross-family merge-consensus receipt `455cb89d5fb71c09`).
