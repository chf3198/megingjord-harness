### Fixed
- `pretool_guard.py` command-CONTEXT gates (create-PR, issue-close, merge, checks, publish/release, epic-close, no-code-admin) no longer false-block a tool call when the matched command STRING is quoted inside a `--body`/here-doc of an issue-comment or artifact-posting call (the #3631 collision, which fired twice — including while posting the #3661 MANAGER_HANDOFF). They now scan the #3471-sanitized command (quoted spans masked, here-doc bodies stripped); unquoted REAL commands still fire, and safety/content scans (dangerous-command, secret, fleet-curl, redirect) keep the raw command. Refs #3661.
