- Harden collaborator pre-handoff self-check: COLLABORATOR_HANDOFF structured-field
  validation now flows through a single source-of-truth schema module
  (`scripts/global/collaborator-handoff-schema.js`), shared with the server-side
  megalint gate so the two parsers cannot drift. Prose-content checks remain
  documented heuristics. (#1580)
