- fix(governance): block Epic close when children are non-terminal (#3350). The
  `issues.closed` backstop now matches children via the full union — task-list,
  native sub-issue, `Parent:` text, AND cross-ref children whose own body asserts
  parentage (`Refs Epic #N`) — closing the gap that let #3021/#2891 close with
  open children. Adds CONSULTANT_CLOSEOUT children-terminal reconciliation
  (catches the #3021 false-claim class), a flap-safe eventual-consistency
  re-check before reopen, a schema-v3 `incidents.jsonl` signal
  (`pattern_id: epic-closed-with-open-children`), and a local pre-close guard
  (`epic_close_guard.py`) that blocks the operator close path with an audited
  `EPIC_CLOSE_OVERRIDE=1` / `[epic-close-ok]` escape hatch.
