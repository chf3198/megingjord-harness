### Cross-runtime state-injection guard (#1936)

Added `scripts/global/cross-runtime-injection-guard.js` + adversarial corpus + self-test for the cross-runtime injection vectors the #3021 LOCAL-only cluster left open: (1) lease-state reconcile against the authoritative registry, (2) hook-context env-trust (enrolled team + safe session id), (4) baton-artifact sovereignty (artifact team == PR team; signer enrolled). Vector 3 (signer substrate-first) is caught at validation by the sovereignty enrollment check. Refutes the Epic's "superseded" hypothesis with the residual implemented. Pure + dependency-injected; advisory-first. Tests + stress (G6 randomized fault-injection + G7 p99).
