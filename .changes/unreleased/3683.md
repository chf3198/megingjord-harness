### Fixed
- Restore green on `tests/consultant-closeout-required-fields.spec.js` by adding
  `worktree_residual_risk: none` to the two stale closeout fixtures (shared
  `minimalBody()` + the inline advisory-case body) that predated the
  `wtGate.checkConsultant` worktree-residual requirement. Fixtures-only; no
  validator behavior change. (#3683)
