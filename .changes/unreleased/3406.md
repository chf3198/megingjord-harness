- **Goal Constitution — operator-autonomy principle (Epic #3391).** Added operator autonomy to
  `instructions/harness-goals.instructions.md` as a cross-cutting, always-on, proportional PRINCIPLE
  (Option B; not a ranked goal): resolve reversible/low-risk work autonomously via the free cross-model
  panel, reach the human only at the 4 retained carve-outs (design/UAT/irreversible/security-weakening),
  HARD-subordinate to C-G1 and C-G4, with G8 decision-logging and a Decision-Lens entry. Propagated
  atomically to every goal-list consumer (global-standards, owasp-agentic-mapping, test-methodology-matrix,
  goal_lens.py, governance-context.js) and wired an autonomy dimension into the governance-bundle rubric,
  the cross-model raters (second-opinion-runner GA score), and the fleet-decision-oracle. Defused the
  byte-identity trap by extending `lint-goal-canonical-identity.js` to anchor through G10 Maintainability
  (was silently truncating at G9). A drift test (`tests/goal-autonomy-drift.spec.js`) asserts zero
  consumer drift. Closes #3406 #3409 #3407 #3408.
