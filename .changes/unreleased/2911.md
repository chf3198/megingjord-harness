### Security

- `scripts/global/megalint/manager-handoff.js`: promoted `cross_runtime_writes` field from advisory to hard-blocking gate (#2911). When a PR diff touches `.claude/`, `.codex/`, or `.copilot/` paths, a missing, empty, or pending-sign-off `cross_runtime_writes` field in the MANAGER_HANDOFF now blocks baton advancement with severity `hard`. Configurable path list in `config/governance-rules.yaml` under rule `cross-runtime-writes-required`. Scope-correct: tickets that genuinely do not touch cross-runtime paths are unaffected.
