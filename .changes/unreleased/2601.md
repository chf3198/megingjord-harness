### Changed

- Fleet review dispatch (`fleet-red-team-dispatch.js`) now sends a configurable `keep_alive` (default `30m`, override via `FLEET_KEEP_ALIVE`) on its Ollama `/api/generate` calls, with invalid/empty values safely falling back to `30m`. This keeps the model resident between baton reviews and reduces paid fallback risk (G3) (#2601).
