## feat(skills): Copilot skill-invocation wiring via runSubagent and MCP (#3047)

Exposes 7 governed harness skills as Copilot-invokable through a new
`megingjord-skill-invoke` MCP server and `buildInvocation` helper.
Wraps existing `~/.copilot/skills` entries — no reimplementation.
Ships `.github/copilot-mcp.json` per-repo config, `docs/howto/copilot-skill-invocation.md`,
and full tdd-pyramid test coverage.
