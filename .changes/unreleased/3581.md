- **feat(governance):** ruleset profile pinning (Epic #3576 W-G) — `ruleset_version:`/`ruleset_uplift:` in MANAGER_HANDOFF freeze a ticket's validation profile at start so a blocking rule landing later cannot retroactively invalidate in-flight artifacts (#3532 class); security-critical (#2892 Tier-H hard-floor) rules stay immediate-effect (G4). `scripts/global/ruleset-profile-pin.js` resolver + `config/ruleset-version.json` + advisory validator wired to megalint. (#3581)
