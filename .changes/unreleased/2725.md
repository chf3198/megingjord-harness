### Added
- **Governance-chain authoring UX + health check** (#2725, Epic #2709): `scripts/global/gov-check.js` runs the keystone meta-validator locally and reports per-chain health (`--json` for a dashboard), giving authors the same verdict CI gives before they push (shift-left). `scripts/global/gov-scaffold-link.js` generates a compliant registry entry + guard stub and refuses `operator-discretionary` at authoring time (poka-yoke). `docs/howto/governance-chains.md` documents the invariant and the add-a-governed-link workflow. (The visual dashboard panel rendering is a tracked follow-on; the `--json` health feed is the data source.)
