- **Self-dev parity-lowering guardrail (#3452, Epic #3411 T3.2):** `scripts/global/megalint/parity-lowering-guard.js` (`npm run parity:guard`) recomputes the T3.1 parity matrix and blocks any net regression (a `full`→`partial`/`absent` flip, or an `ok`→`declared-full-but-missing` drop) against a committed `tests/fixtures/parity-baseline.json` monotonic-parity baseline — an approved tested-substitute waiver (a `waived`/`structural-NA` cell with a non-empty `substituteTest`) is the only escape. Advisory-first (`--strict` opt-in); G6 absolute fallback (missing/unbuildable baseline → no-op, never crash); `npm run parity:baseline:update` is the audited intentional-change path. Regression anchor: a deliberate `full`→`absent` flip is blocked. Stress: fault-injection + p99 (0.7ms).
