### Changed
- `hamr-bypass-lint.yml` workflow promoted from advisory to required gate per #1157: sets `HAMR_BYPASS_GATE=1` env on the lint step (switches the script out of advisory mode), removes `continue-on-error: true` (makes a detected bypass a blocking failure), drops the "(advisory)" suffix from workflow + job display names. Stability evidence at promotion: 29/30 recent runs successful (#1158 close cycle measured). Required-status-checks list update applied separately via `gh api PATCH` in Admin phase post-merge (AC3).
