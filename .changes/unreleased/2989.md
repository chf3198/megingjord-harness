### Added
- Governance drift sweeper now auto-remediates the deterministic safe drift classes via `npm run governance:drift-sweep -- --fix` (D4 strip `resolution:*` on open issues, D5 `status:backlog`→`status:queued` for active-Epic children, D8 strip `phase-gate:phase-1` from Epics, D3 strip commit/bracket title prefixes). Dry-run is the default; mutations require explicit `--apply` and are recorded to an append-only JSONL log that `--rollback <run_id>` can reverse. Runs at $0 token cost (pure `gh` JSON + deterministic rules; no LLM). D1/D2/D6/D7 remain propose-only. (#2989)
