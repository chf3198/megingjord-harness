- **Fleet Advisor Layer-① lint engine (#3480):** new deterministic, offline-safe, $0 fleet audit — `scripts/global/fleet-advisor-lint.js` + data-driven rule table `config/fleet-advisor-rules.yml` (14 rules). Builds a collision-safe fleet fingerprint, classifies fleet-configuration tier F0→F4 (degrade-safe: ambiguous → lower tier), and evaluates the rule table (multi-engine detection, transient-fault rule). Never throws — a dead host is a finding, not a crash. Rule docs auto-generate from the YAML (`npm run fleet-advisor:rules-doc`); a CI rule-coverage gate asserts every rule is exercised across F0–F4 fixtures. Foundation for the Fleet Advisor capability (Epic #3414).
