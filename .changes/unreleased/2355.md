### Fixed

- `scripts/sync.sh` now refuses canonical-main writes (`${HOME}/devenv-ops/`) unless `--allow-canonical-write` is passed, preventing the reverse-sync regression of merged source files demonstrated by the 2026-05-27 22:14:10 incident (16 files reverted across PRs #2304 + #2308). The guard emits a v3 incident JSONL event on trip; see `instructions/global-standards.instructions.md` Canonical-main checkout policy section for the trap rationale. (#2355)
