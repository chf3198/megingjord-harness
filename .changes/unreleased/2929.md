### Changed
- Probe-first LiteLLM→Ollama fleet routing (#2929, Epic #2926 C3): fleet dispatch now runs a fast (~5s) LiteLLM gateway health probe *before* dispatching, and routes straight to direct Ollama when the gateway is down/slow — eliminating the 120s hard-timeout hang operators previously had to wait through. The previously-silent LiteLLM→Ollama fallback is now visible on stderr (`[fleet] litellm <reason> → direct Ollama fallback`) and recorded to model-routing telemetry. New `scripts/global/fleet-backend-select.js` (`selectFleetBackend` + `dispatchFleet`); `litellm-client.js#dispatchFleet` is additive (`chatComplete`/`healthCheck` unchanged); canonical path documented in `config/red-team-model-matrix.yml`. G6 resilience + G8 observability keep the $0 fleet lane usable when the gateway flaps.
