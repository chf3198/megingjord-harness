- **docs(governance):** ADR-021 — declare the operative ruleset is the working tree; mandate a verbatim rescue snapshot (rescue/ branch+tag, plumbing-preserved) before any hook/instruction-touching git op, generalized across all team runtime surfaces incl. non-git `~/.codex` / `~/.antigravity` (Epic #3576 W-A A2; #3580). Rescue snapshot `copilot-governance@rescue-pre-optimization-20260703` preserves 5728 files of previously-unversioned live policy (E9/E12).
- **feat(hooks):** anneal-lane carve-out in `commit_ticket_gate.py` — commits on a rolling `anneal/<ISO-week>` branch may reference an `incident:<pattern_id>` in lieu of a `#N` ticket ref; linkage stays required (Epic #3576 W-A A3; #3580). Unit tests: `tests/hooks/test_commit_ticket_gate_anneal.py` (8 cases).
- **feat(scripts):** `runtime-dir-rescue.sh` brings a team's live-policy runtime dir under local version control via default-deny `.gitignore` + policy allowlist + fail-closed staged-credential backstop; applied to `~/.codex` + `~/.antigravity` (previously non-git — E12), secrets (`auth.json`, `*.sqlite*`, sessions) excluded (Epic #3576 W-A A5; #3580).
