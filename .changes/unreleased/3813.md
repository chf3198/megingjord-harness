- **Honest-degradation wrapper for externally-coupled gates** (Epic #3807 C5, #3813): new
  `scripts/global/honest-degradation.js` gives any gate whose input is external liveness a
  `reachable?` pre-check distinct from its `pass?` check — an unreachable dependency now yields a
  visible `cannot-verify` advisory (operator-visible signal, never a hard block on completed work)
  instead of a false failure. Retired the redundant `MEGINGJORD_MCP_FORCE_AVAILABLE` bypass flag
  (a "force MCP" knob that asserted availability without probing it); `github-dispatcher` now resolves
  MCP-vs-gh-CLI through the wrapper's honest availability default. Net governance-surface Δ: −1
  (one bypass flag retired; the new module is a global script, not counted surface). No G1/G4 control
  weakened — MCP and gh-CLI post identical governance artifacts (transport interop, G9).
