- **Prose relative-link checker (#3297)**: new `prose-link:check` verifies relative `.md`→`.md` links in hand-authored prose (`docs/`, `instructions/`, `research/`, `wiki/wisdom/`) resolve on disk — closing the audit-#3296 (AC3) gap between `docs-anchors` (doc→code) and `docs-health` (orphan/stale). Fixed 19 broken relative links (wrong depth / moved wiki targets) across `docs/howto/` and `research/`. Auto-generated wiki mirrors (`wiki/work-log`, `wiki/code`) are excluded (pipeline-owned). Advisory CI gate; boundary documented in `docs/howto/prose-link-check-boundary.md`.
