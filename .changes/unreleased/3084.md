### Added
- Cursor is now a registered governed runtime (Phase 0 of Epic #3083, #3084): new `.cursor/rules/megingjord.mdc` thin adapter (the 4 invariants + goal order, `alwaysApply: true`), mirroring the shipped Antigravity precedent. `cursor` is registered across all six parity surfaces — `governance-adapter-emit` (target + `.cursor/rules/<id>.mdc` path + Cursor-native `description`/`alwaysApply` frontmatter), `cross-team-contract-check` ENTRY_POINTS, `xteam-mcp-register` (`~/.cursor/mcp.json`, `mcpServers` key), `deploy.sh` (target + rsync), `orchestrator-governance-parity.json` runtimes, and `team-model-signatures.json` (teamValues + `cursor-ide` substrate + `Cyrus <role>` alias registry). New `npm run deploy:cursor` / `deploy:cursor:apply`; `docs/howto/cursor-worktree-pattern.md` and a Cursor adapter section in `provider-neutral-governance`. Runtime/HAMR/signing slug is `cursor` (assignee slug stays `cursor-team`). Design cleared an iterative cross-model consensus above 93/100 (Llama-3.3-70B 98 + qwen 95). Hook enforcement and HAMR substrate are deferred to Phase 1 (#3085) behind a documented waiver in the parity manifest.
