- **Prompt-artifact structural linter (#3302)**: new `prompt-artifact:lint` checks skill/agent prompt artifacts (`skills/**/SKILL.md`, `agents/**`, `.claude/commands/*.md`) for broken relative links, missing `name`/`description` frontmatter, and stub bodies — closing the audit-#3296 seam between eval-harness (behavior) and doc-coverage (prose). Advisory CI gate; fixes the dead links in `mem-watchdog-ops` and `repo-standards-router`. Boundary documented in `docs/howto/prompt-artifact-lint-boundary.md`.
