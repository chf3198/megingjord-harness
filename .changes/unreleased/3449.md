- **Copilot detection deltaKind recognition (#3449, Epic #3411 T2.6):** closes the #3041 Copilot detection gap at the descriptor layer with NO `detect-runtime.js` logic change. The runtime-descriptor schema now conditionally requires `aiAgentValue` when `detection.deltaKind == "ai-agent-value"`, and `runtime-descriptor.js` gains `validateAiAgentValueDetection()` — an explicit positive check that a marker-less runtime (Copilot) is covered by the AI_AGENT-value detection path, replacing the previous silent skip. `tests/copilot-detection-deltakind.spec.js` (8 tests) pins it.
