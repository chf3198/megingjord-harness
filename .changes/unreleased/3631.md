- **feat(harness):** add `harness:confidence` — a uniform cross-team runtime-capability probe (`npm run harness:confidence`) emitting the Codex-co-designed `harness-confidence/v1` JSON envelope with per-probe evidence (env hydration, HAMR, free-cloud/fleet smoke, GitHub, skills, tooling, deploy parity). `--quick` (default, zero-network) / `--smoke` (live). Secret-safe (`present|missing` only), graceful degrade, deterministic `overall` derivation. Detects the keyless-worktree free-cloud defect. (#3631)
  - Usage: `npm run harness:confidence` (quick, zero-network) or `-- --smoke` for live free-cloud/fleet evidence; `-- --json` for machine-readable output.
