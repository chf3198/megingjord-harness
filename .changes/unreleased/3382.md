### friction-classifier — route recurring friction to guardrails, not memory (#3382)

Phase-1 of Epic #3380. New `scripts/global/friction-classifier.js` deterministically routes a
Tier-2 friction record to `{guardrail-candidate, skill, semantic-memory, forget}` so recurring
deterministic friction becomes a guardrail instead of a context-growing memory note. Fail-open to
memory; judgment wins on collision. Ships with a labeled corpus + replay-eval (precision ≥ 0.85
promotion gate, no calendar threshold).
