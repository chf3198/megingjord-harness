### Fixed
- shared governance label rules now reject open issues that carry terminal `status:done` or `status:cancelled`, closing the remaining AC2 gap from Epic #2197.
- added regression coverage for open-terminal status violations in the label-rules test suite.
