- **fix(governance):** resolve the E4 anchor contradiction (Epic #3576 W-B) — the governance anchor in `precompact_anchor.py` + `session_context.py` now reads "one branch = one ticket = one PR **by default**; multi-close batching per the #1714 Multi-Close contract" instead of flatly forbidding bundling. Adds `scripts/global/lane-selector.js` (diff-size/paths/risk → recommended lane), `scripts/global/batch-block-generator.js` (lead + sibling brief-evidence blocks), and `docs/howto/lane-selection.md` decision table. (#3583)
