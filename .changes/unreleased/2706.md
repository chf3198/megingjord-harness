### Added
- **Admin-override bypass pre-flight guard + auto-Tier-1 bridge** (#2706, Epic #2709): `pretool_guard.py` now denies an `--admin` override merge unless the Epic #2517 exception is already recorded on the active ticket (fail-closed; the `try/except` prevents a crash so a guard bug yields a recoverable deny, never a session brick) - converting the bypass-exception link from post-merge-only to pre-flight. `scripts/global/gate-failure-tier1.js` auto-emits the Tier-1 incident on an operator-caused gate failure so self-anneal escalation no longer depends on the operator remembering to log it (the #2703 break).
