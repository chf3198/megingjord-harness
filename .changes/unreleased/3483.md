- **Fleet Advisor background trigger (#3483):** new `scripts/global/fleet-advisor-trigger.js` — a bounded, non-blocking SessionStart/cron entry that runs the deterministic lint ($0) and gates the AI-research pass on a **fingerprint-changed-or-stale** check against a small cache, so an unchanged fleet triggers no AI dispatch and costs zero tokens. Kill-switches (single dispatch, token cap, timeout) bound the AI pass; `FLEET_ADVISOR_DISABLED=1` is a clean no-op. Part of the Fleet Advisor capability (Epic #3414, §5).
