- **Remediate 5 operator-resolvable client-prompt surfaces in `pretool_guard.py`** (#3402, Epic #3392 AC2): the raw-fleet-curl (S1), gh-issue-close-before-label-normalize (S2), PR-before-commit (S3), CI-checks-before-PR (S4), and integrity-before-publish (S5) surfaces no longer emit `permissionDecision: "ask"` (which overrides bypass mode and interrupts the client). They now self-resolve — S1/S2 deny with an operator-actionable redirect, S3 is state-derived (allow when a real commit is ahead of base, else redirect to commit), S4/S5 allow read-only sequencing with an advisory. The anti-goal is preserved: S1 keeps its HAMR-bypass deny + incident and `# hamr-bypass-ok` carve-out, S2 keeps its label-normalization requirement, and the two security-sensitive surfaces (S6 hook-mutation, S7 sensitive-path) still ask (handled by #3403). No security/governance control was removed.
