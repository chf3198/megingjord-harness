### Fixed

- Worktree tool boundary: file-editing tools restricted to registered workspace;
  shell commands required for non-workspace worktree paths (#3243).
  Covers all runtimes: Antigravity, Copilot, Claude Code, Codex.
  Deployed via `instructions/worktree-tool-boundary.instructions.md` (`applyTo: "**"`),
  wired in `CLAUDE.md` and `.codex/AGENTS.md`. Refs #3243 #3242.
