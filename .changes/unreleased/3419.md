### Added
- `inbound-reference-integrity` (#3419, Epic #3398 C1): a new `issues.closed` GitHub Action (`.github/workflows/inbound-reference-integrity.yml` → `scripts/global/inbound-reference-integrity-check.js`) that is the **inbound** sibling of `epic-close-readiness-check.js` (which is outbound-only). On close/cancel of `#N` it scans live OPEN issues for dangling pointers **to** `#N` per a reference-form catalog (`merge into`, `blocked by`, `survivor/canonical/supersedes`, `Refs Epic/Parent`), auto-files a Manager-triage `type:correction` re-home task, posts a `BLOCKER_NOTE`, and emits a schema-v3 `incidents.jsonl` event (`pattern_id: inbound-reference-orphan`). Folds in the $0 deterministic **PB4 dependency-rot** class (a `blocked by #N` edge cleared when `#N` closes). Pure logic in `scripts/global/inbound-reference-integrity.js`; deterministic, no model calls (the semantic lane is #3420). Detection routes drift **into** the baton rather than dying as a report (Epic #3398 AC2/AC3). Ships advisory per the #3398 replay-eval promotion model. Tests: `tests/inbound-reference-integrity.spec.js` + `tests/stress-inbound-reference-integrity.spec.js` (adversarial corpus + p99 budget).
