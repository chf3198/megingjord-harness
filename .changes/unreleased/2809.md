# Dashboard tri-mode transport + demo mode + GitHub Pages deploy

- Added `transport.js` — runtime IS_DEMO detection via `?demo=1` param or `github.io` hostname
- Added `transport-mocks.js` — overrides all data-fetch globals in demo mode with plausible static data
- Added `fixture-runner.js` — JSONL scenario replay engine driving activityLog and batonState
- Added `demo-overlay.js` — scenario picker, demo banner, concept glossary (7 harness terms)
- Added `css/demo.css` — demo mode UI styles (purple accent)
- Added `dashboard/fixtures/` with 5 named scenarios: healthy session, governance alert, cost routing, multi-agent coordination, MCP discovery
- Added `.github/workflows/gh-pages.yml` — auto-deploys `dashboard/` to GitHub Pages on `main` push
- Patched `index.html` (3 line-append changes) and `event-source.js` (IS_DEMO guard)

Refs #2809 | Epic #2803
