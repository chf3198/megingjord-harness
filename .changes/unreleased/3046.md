### Added
- `.github/copilot-instructions-tool-reliability.md`: terse anti-hallucination directives requiring
  Copilot to read live ticket/repo data via tools (`gh issue view`, `read_file`, `gh pr view`) before
  authoring governed artifacts, and explicitly forbidding invention of `#N` issue IDs or fabrication
  of artifact content (#3046, Epic #3041).
- Link from `copilot-instructions-advanced.md` §"Tool-Use Reliability" to the new directive file.
- `AGENTS.md` tool-use reliability note pointing to the directive file.
- `tests/tool-use-reliability-doclint.spec.js`: 6 drift-lint assertions ensuring directives remain
  present, correctly sized, and linked.
