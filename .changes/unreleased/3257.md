- **Governance — baton-back state primitive (#3257):** Added `scripts/global/baton-back.js`, the deterministic baton-back routing core (remediation routing matrix, open/clear marker, bounded non-blocking escalation) plus a server-side issue/PR-timeline mirror with local rebuild-by-replay. Wired the close-gate invariant into `closeout-preflight.js` so a ticket cannot close while a baton-back marker is still open (enforced only when the closeout itself is enforced).
