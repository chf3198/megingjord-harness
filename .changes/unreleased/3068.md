### Added
- Wiki-health detector (#3068, completes Epic #3063 — the anti-recurrence core for the 2026-06-16 "both Three-Wiki stores empty + pipeline silently failing" meta-failure). New `scripts/wiki/wiki-health-detector.js` aggregates the existing wiki modules (`drift-detector`, `health-contract`, `reconcile`, `replay-eval-harness`) into a per-store (A=code, B=work-log) health vector — `coverage_ratio` / `stale_ratio` / `consistency_errors` / `reconcile_error_rate` / `actions_minutes` — emits a schema-v3 event tagged with goal G8 to `dashboard/events.jsonl`, and classifies advisory vs a `wiki-store-empty-or-stale` Tier-2 incident (coverage 0 while sources exist). The `#2058` wiki-drift gate's advisory→required promotion is now replay-eval-precision-gated (≥0.85, auto-revoking — not calendar), mirroring the #3105 test-floor model; the workflow stays advisory until the corpus calibrates. Run `npm run wiki:health`. Covered by `tests/wiki-health-detector.spec.js` (tdd-pyramid) + `tests/stress-wiki-health-detector.spec.js` (fault injection + p99). Live dogfood: Wiki B healthy, Wiki A at 0.90 coverage (advisory — a real minor gap surfaced by the detector).
