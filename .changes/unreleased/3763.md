- **Wiki frontmatter schema reconciled to the validator + required PR gate (#3763, Epic #3719 P1-e).**
  `config/wiki-frontmatter.schema.json` is now the documented source of truth (`type`/`updated`, 4-value
  enum); `instructions/wiki-knowledge.instructions.md` no longer claims the retired
  `wiki_type`/`freshness_window` contract. New `wiki-frontmatter-gate` workflow validates changed wiki
  pages on every PR (nav files exempt; no-op green on non-wiki PRs) and is a required check so schema
  divergence cannot re-accrue. Historical-corpus backfill tracked in #3767; `freshness_window` retirement
  owned by #3731.
