### Added
- Velocity-relative exempt-item review sweep (F3 of Epic #3517, per ADR-020 §D2): new weekly `exempt-review-sweep.yml` + pure cores `scripts/global/median-cycle-time.js` and `scripts/global/exempt-review-sweep.js` that surface `signal:exempt-review` on exempt-but-idle epics sitting in the `stale.yml` exemption blind zone (never reviewed). The idle threshold is `max(45d, 3 × median child cycle-time)` — velocity-relative per the #2983 anti-calendar critique — with a cold-start guard (<5 valid samples → 45d floor) and a degenerate-median guard (burst same-day closes → 45d floor, never trips early). Surface-only invariant I0: never closes. Reuses the #2920 dormant scaffold and **defers to #2920** for `status:dormant` epics; skips epics already carrying `signal:close-eligible` (D1). Ships in SHADOW/report-only mode first (`EXEMPT_SWEEP_SHADOW`, logs would-be targets, applies no label); kill-switch `EXEMPT_SWEEP_DISABLED=1`. `signal:*` namespace (D-0002). Tests + ADR worked-example fixtures + a stress spec (p99 budget + chaos path).
