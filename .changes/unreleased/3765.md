- **Wiki sustained-proof loop — proving upkeep HOLDS, not asserted once (#3765, Epic #3719 capstone).** Every
  prior wiki generation shipped `completed` once, then rotted. A new scheduled `wiki-sustained-proof` monitor
  measures the fresh wiki-mirror work-log health each cycle (coverage vs 0.95, staleness vs 0.10, reusing
  `wiki-health-detector`), records it to the committed non-gitignored `governance/wiki-sustained-proof.jsonl`
  (cannot self-silence), and `checkSustained` asserts the last N=5 cycles all hold; a failing cycle hard-fails +
  files a durable issue. Current cycle passes on the fresh surface (coverage 1.00 / staleness 0.00). N=5 is a
  replay-eval count that accrues over reconcile cycles (anti-calendar); the drift/lint advisory→required flip is
  eligibility-gated on `checkSustained`, not a calendar. Exit-criteria scorecard: `research/wiki-durability-exit-criteria-3765.md`.
