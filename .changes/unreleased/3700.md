### Fixed

- **Closeout finality (Epic #3679):** a `CONSULTANT_CLOSEOUT` carrying `rubric_provisional: true`
  is now BLOCKING (`rubric-provisional-not-final`) — a provisional (non-final) rubric must not pass
  the closeout gate; the Consultant must commit a final G1-9 verdict. (#3700)
