### Fixed
- `closeout-preflight.js` no longer demands a `CONSULTANT_CLOSEOUT` during an ACTIVE-review rework push per #3636: when the PR's latest review decision is `CHANGES_REQUESTED` or the PR is a draft, the closeout is correctly absent (review not re-approved), so enforcing it at pre-push inverted the baton and pushed operators toward bypass envs. The exemption relaxes ORDERING only — close-time enforcement (CI `consultant-gate`, `merge-evidence-pr-gate`, and the `pretool_guard` close gate) is unchanged, and a closeout that IS already posted stays validated.
