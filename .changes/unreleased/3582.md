- **feat(governance):** close-atomicity gate (Epic #3576 W-C, E10) — `scripts/global/close-atomicity-gate.js` blocks a completion claim until the re-read tracker state is terminal (issue CLOSED + single status:done/cancelled + no role + no open baton-back), guarding the #3529 open-done race; builds on the #3284 FSM. Plus `buildArtifactSet()` batched multi-artifact build ("single command"). Advisory validator wired to megalint. Unit + stress (chaos + p99) tests. Edit-in-place / builder-mandatory enforcement ship advisory (promotion-deferred). (#3582)
