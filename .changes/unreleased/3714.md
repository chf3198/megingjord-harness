### Added
- Operator self-post `CONSULTANT_CLOSEOUT` grant + decision-routing (#3714). `BATON_AUTOMODE.autoMode.allow` (`scripts/global/automode-provision.js`) gains a scoped clause authorizing the operator to author a `CONSULTANT_CLOSEOUT` on a PR it itself authored — **only** when the closeout carries a valid `cross_family_verdict` backed by a **verified** cross-family consensus receipt (`governance/cross-family-consensus.jsonl`, ≥2 non-authoring families) AND required CI minus the consultant gate is green; a same-family/no-receipt closeout still blocks (anti-self-approval preserved). Decision-routing documented in `operator-identity-context` + `feature-completion-governance`: a receipt-backed self-post is autonomously-resolvable (route to the free cross-family panel), not a retained client touchpoint — closing the #1889 escalation. The `autoMode` grant install remains environment-owner config (`hamr:activate`).
