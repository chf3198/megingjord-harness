- **Ship gate corpus to cursor + antigravity (#3446, Epic #3411 T2.3):** `deploy.sh` now ships the `scripts/global/**` gate corpus (megalint + governance JS plane) to each runtime's `deploy.gateCorpusHome` (cursor `~/.cursor/scripts/global`, antigravity `~/.antigravity/scripts/global`) — the §2.4 make-reachable resolution. Adds `sync:{cursor,antigravity}[:apply]`, `deploy:manifest:{cursor,antigravity}`, `deploy:verify:{cursor,antigravity}`, `gate-corpus:verify` npm scripts; `hamr-sync-verify.js` now fails when the corpus is missing from either runtime. Ship-corpus chosen over MCP-delivery (deterministic, no live dependency; free cross-model panel adjudicated). Unit + stress tests (atomicity chaos + p99).
