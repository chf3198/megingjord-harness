- **Gate-corpus artifactClass (#3443, Epic #3411 T1.6):** added an optional `deploy.gateCorpusHome` field to the runtime-descriptor schema and declared the `scripts` artifactClass + gate-corpus home for cursor (`~/.cursor/scripts/global`) and antigravity (`~/.antigravity/scripts/global`), making the `scripts/global/**` JS gate plane a reachable deploy surface (make-reachable, not tier-down). The `megalint-orchestrator` catalog feature's cursor/antigravity cells move unverified → partial with a promotionPath to T2.3 (#3446), which wires the actual deploy.
