- **Cursor onboarding completeness dogfood (#3447, Epic #3411 T2.4):** dogfooded the T2.1 scaffold against Cursor — `harness:add-runtime --runtime cursor --dry-run` reports 0 pending across all 11 core surfaces, confirming Cursor is fully wired with no manual per-surface edits. `tests/cursor-onboarding-complete.spec.js` + `tests/fixtures/cursor-onboarding-golden.json` lock parity as a regression anchor. The three #3086 surfaces still absent (skills adapter dir, state_store deployment, reverse sync) are documented `parityWaivers` in the cursor descriptor with explicit promotionPaths — out of scope for the generator.
