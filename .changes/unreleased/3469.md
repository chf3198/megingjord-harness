Fixed the pre-push gate false-blocking `git push --force-with-lease` after a rebase or under cwd-churn (hook cwd on the main checkout while the committed branch lives in a sibling worktree). The commit-step check now re-derives the signal from live git across all linked worktrees, and a branch-change state reset no longer wipes the commit signal when a worktree still holds a genuine commit ahead of base — eliminating the manual hook-state patch workaround. (#3469)
