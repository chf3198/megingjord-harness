### Fixed
- Review dispatchers (dispatchRedTeam, collaborator-preflight) now fail over to the $0 free-cloud tier when the fleet (Ollama) is unreachable, instead of returning an empty review — closing the gap where a fleet outage forced a review-skip or a paid escalation (G3). New scripts/global/review-dispatch-failover.js (shared by both dispatchers); availability-only trigger (capability failures do not fail over); cross-family validity preserved; lane:free-cloud telemetry emitted; graceful advisory degrade when free-cloud is also down. Refs the #2619/#2621/#2624 failover contract.
