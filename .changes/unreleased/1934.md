### Cross-runtime state_store parity (#1934)

Mapped the per-runtime state_store surface (lease/lock/audit-log/state across `.claude` `.copilot` `.codex` `.megingjord` + antigravity/cursor) into the orchestrator parity manifest and taught `governance:orchestrator-parity` to verify it. Adds an advisory unmapped-runtime detector, a per-runtime audit map, and tests + stress (G6 chaos + G7 p99). Records the Claude-Code "not-deployed" state gap. Reuses existing lease/lock primitives — no re-implementation.
