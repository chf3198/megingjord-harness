- **Retained human-touchpoint registry + new-surface validator** (#3404, Epic #3392 AC3): new `config/retained-human-touchpoints.json` enumerates the 4 sanctioned human carve-outs (design-direction, UAT-confirmation, irreversible-destruction, security-policy-weakening) plus the bounded set of sanctioned `emit("ask")` surfaces mapped to a carve-out. New `scripts/global/megalint/client-prompt-surface-check.js` scans the hook layer for client-prompt surfaces and flags any NOT covered by the registry (`unregistered-client-prompt-surface`), so a new client-prompt cannot be added silently. Ships **advisory** (`--strict` opts into a non-zero exit); promotion to a blocking gate is replay-eval-gated, never calendar. Fail-open.
