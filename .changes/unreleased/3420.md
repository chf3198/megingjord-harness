feat(governance): opt-in `--semantic` cross-model supersession lane on governance-drift-sweep (#3420, Epic #3398 C2)

Flags dormant backlog tickets whose goal is already satisfied by shipped work, with cited shipped-artifact evidence. Fleet-first $0 (fleet → free-cloud → deterministic floor; never a paid tier); default `--scan` stays deterministic $0. Median-of-N + minority-veto; evidence-binding with acyclic transitive-chain validation; G4 redaction before dispatch; C1 inbound-sequencing gate so the sweep never creates orphans (a ticket with live inbound pointers is PARTIAL, never superseded).
