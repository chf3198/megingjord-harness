- **Active client-defer → adjudication redirect (#3749):** `hooks/scripts/client_arbitration_guard.py` no longer merely detects and logs a narrow conflict-keyworded client-defer — it recognizes **any** non-carve-out deferral of an internal decision to the client (broadened to `FORBIDDEN_ASK AND NOT human_carveout`) and the Stop hook (`stop_reminder.py`) **actively redirects** it into the shipped free cross-model panel `adjudication-guardrail.decide()`, emitting a `client-defer-routed-to-adjudication` incident (G8) and naming the exact invocation the operator must run. The 4 human carve-outs (design/UAT, irreversible, security-weakening) remain the sole sanctioned client-escalation path; detection is fail-safe (never crashes the Stop hook). Mirrors the in-process adjudicate-first precedent in `pretool_guard.py` S6/S7 (#3403); complements the execution-state `stuck-state-detector.js` (#3748). R3 of #3059.
