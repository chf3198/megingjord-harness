- **Wiki retrieval reads fresh work-log mirrors from `wiki-mirror` (#3779, Epic #3719, completes #3729 AC2).**
  The reconcile lands fresh mirrors on the non-protected `wiki-mirror` branch (main's ruleset rejects direct
  pushes), but retrieval read the frozen `wiki/work-log/` on main. `scripts/wiki/mirror-source.js` materializes
  `wiki-mirror:wiki/work-log/` into a gitignored cache and `hybridSearch` now prefers it, with a graceful
  Tier-0 fallback to local when wiki-mirror is unavailable (never throws). The reconcile/health *write* path
  keeps reading local `wiki/` (no circular dependency). Verified: ticket 1271 now retrieves its fresh
  `2026-07-13` content (was frozen at `2026-06-17`); the retrieval ticket ceiling rose 3029 → 3200.
