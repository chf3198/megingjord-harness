- **Governance surface pruning (#3820, Epic #3807 C6 Option B):** retired the CI-dormant `signer-format-canonical` validator by relocating its two checks — the Epic #1526 role-prefix-as-provenance anti-pattern and the canonical `Team&Model` format check — into the CI-wired `signer-fidelity` gate as advisory findings. The checks move from *never enforced* to *enforced-in-CI*, a strict strengthening of the anti-spoofing property; no previously-passing artifact is newly blocked (promotion to blocking is replay-eval-gated). Removes one counted governance-surface unit. Client-authorized security sign-off (retained touchpoint) for retiring the G4 control.
