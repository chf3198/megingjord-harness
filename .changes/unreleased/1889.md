### Added
- `completion-claim-truth` megalint validator (#1889, Epic #3398): an advisory `issue_comment.created/edited` guardrail that detects completion CLAIMS in a comment whose cited artifacts are not actually on the default branch — each cited file path is checked for existence at `origin/main` HEAD (`repos.getContent`, ref=default), and each cited `PR #N` is checked for a non-null `merged_at` (`pulls.get`). Dangling claims produce one structured advisory comment (never blocks; #1612 advisory→required promotion path). Detection is pure/injectable (`detect` + `classify`), future-tense/prose mentions are excluded to avoid false positives, and an adversarial-input stress spec asserts a p99 < 5ms/100-line budget (G7) plus fault-injection paths (G6). Closes the class behind seed incidents #1874 (stale PR merge-vehicle reference) and #1399/#3319/#1271 (deliverable claimed complete but absent from main). Files: `scripts/global/megalint/completion-claim-truth.js`, `.github/workflows/completion-claim-truth.yml`, `tests/completion-claim-truth.spec.js`, `tests/stress-completion-claim-truth.spec.js`, self-test registry id `completion-claim-truth`.
