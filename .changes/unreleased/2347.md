- **Governance — explicit accountable-team schema** (#2347, Epic #2345): added an `accountable-team:<team>` label namespace that records persistent team-of-record accountability, distinct from the transient `role:*` baton label and persisting across all states including terminal. New `scripts/global/accountable-team.js` (resolution helper + manager/admin authority rule) and `scripts/global/accountable-team-backfill.js` (dry-run-default, idempotent migration). Documented in `docs/howto/accountable-team-schema.md`.
