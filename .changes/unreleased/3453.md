- **Mandatory cross-family review on the SSoT (#3453, Epic #3411 T3.3):** `fleet-review-required.js` now requires a cross-family review on any PR touching `inventory/harness-feature-catalog.json`, `inventory/runtime-descriptor.schema.json`, or `inventory/runtimes/*.json` — the highest-leverage drift vector gets the strongest independence requirement. Enforces a reviewer whose model family differs from the author's plus a 16-hex `cross_family_receipt` (qwen-32b fleet-first). First real production caller of `fleet-review-required.js`. Contract spec (17 tests).
