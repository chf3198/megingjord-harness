### Changed
- `closeout-preflight.js` (the automatic pre-push gate) now runs at strict parity with CI's baton-gates per #3657: once a `COLLABORATOR_HANDOFF` is posted it runs the FULL `collaborator-handoff` validator (cross_family_* fields, `worktree_behind_main`, Pre-handoff verification block, doc-coverage), plus `changelog-fragment-presence` (#3691 named instance) and `signer-fidelity` per posted artifact. Artifact-FORMAT errors (the #3631 friction cascade) now fail locally BEFORE PR-open instead of one-by-one at CI. The cross-family receipt ledger-membership anti-forgery check (#3678 F1) remains CI/merge-owned; pre-artifact pushes are never false-failed.
