- **Parity-matrix replay-eval promotion gate (#3454, Epic #3411 T3.4):** `tests/fixtures/parity-matrix-corpus.json` (15 samples: genuine-lowering / legit-NA / a `legit-waiver-excluded` exclusion class withheld from the precision denominator) + `scripts/global/harness-parity-replay-eval.js` (`npm run parity:replay-eval`) score the T3.1 matrix / T3.2 guardrail against the corpus, reporting precision/recall and `promotionEligible = precision ≥ 0.85` — auto-revoking, recomputed every run, **never calendar-gated** (per Epic #1771/#1827). Reuses `probeCell` (no logic duplication); `auditRecord` emits a `parity-replay-audit-v1` schema; `PARITY_REPLAY_DISABLED=1` is the rollback no-op. Current corpus: precision 1.0, promotionEligible true.
