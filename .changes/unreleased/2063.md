### Added
- Baton latency aggregation report: `scripts/global/baton-latency-report.js` reads `.dashboard/events.jsonl` baton:* events and reports per-role-transition latency stats (count, mean, p50, p95, p99). Run via `npm run governance:baton-latency:test` for unit tests or `node scripts/global/baton-latency-report.js` for a live report. Refs #2063.
