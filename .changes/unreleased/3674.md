### Governance — closeout-integrity cross-check (#3674, Epic #3669 C5)

`megalint/consultant-closeout.js` gains `checkCloseoutIntegrity`: it flags a
`mid_flight_flaws: none` / `anneal_tickets_filed: none` claim that is contradicted by
>=1 red branch-protection-**required** check on the linked PR (the #1591 false-clean
closeout class). Keys strictly on required-check state, never on comment timing.
Ships **advisory** (never blocks); the caller injects `input.prRequiredChecks`
(no-op when absent), and promotion to blocking is replay-eval-gated (precision >= 0.85).
