- **Validator dispatch contract (#3456, Epic #3411 carve-out):** `inventory/validator-dispatch-contract.json` enumerates all 44 governance validators and the surface(s) that dispatch each, and `scripts/global/validator-dispatch-contract.js` (+ CI gate) fails on any validator with an empty `dispatchedBy` — making a silently-undispatched validator impossible. Rescued 4 orphans (`fleet-review-required`, `registry-tuple-coverage`, `sub-issue-preference`, `worktree-naming-advisory`) by wiring each into the megalint runAll set via advisory adapters. Contract spec (14 tests) is the regression anchor.
