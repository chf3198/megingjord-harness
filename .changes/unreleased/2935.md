### Added
- Runtime-neutral review CLI parity verifier (#2935, Epic #2926 C9): `scripts/global/review-cli-parity.js` locks the D9/G9 invariant — the whole review pipeline (C1 `cascade-dispatch` + C3 `fleet-backend-select` + C4 `fleet-escalation-policy` + C5 `review-stakes-router` + C6 `review-cost-telemetry` + C8 `review-privacy-gate`) lives in the single `scripts/global/` source, so every runtime's review skill invokes the same CLI. `verifyCanonicalSource` proves the 6 modules exist unforked in one place (one shared CLI); `verifyRuntimeParity` content-hashes each module against deployed runtime targets and honestly reports coverage + gaps (golden-file fixture under `tests/fixtures/`). G9 interoperability.
