### Added
- Cursor 5th-runtime Phase 2 (#3086, Epic #3083): completes the #1912 twelve-surface governance parity for Cursor. The `cursor-hooks-emit.js` EVENT_MAP now wires all nine `requiredHookScripts` — adding the ticket-lifecycle gates `manager_ticket_gate.py` + `goal_lens.py` (beforeSubmitPrompt) and `commit_ticket_gate.py` (preToolUse/beforeShellExecution). `deploy:cursor:apply` now also deploys `agents/` to `~/.cursor/agents/`. Cursor is registered as a cross-runtime wiki reader (`~/.copilot/wiki/`) and added to `orchestrator-governance-parity.json` deployTargets + wikiDocsParity. A full twelve-surface Cursor parity matrix is documented in `docs/architecture-runtime-parity.md` (visual_qa and wiki are parity-via-shared-mechanism, consistent with Claude Code / Codex). No open waivers remain for Cursor.
