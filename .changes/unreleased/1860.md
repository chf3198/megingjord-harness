- **Worktree session-lock telemetry** (#1860): the active-session lock now emits best-effort, never-throwing event-schema-v3 lifecycle events (`worktree.lock.acquire` / `refuse` / `replace_stale` / `release` / `lease_expire`) to `~/.megingjord/incidents.jsonl` for G8 observability of lock contention and stale-reclaim. Emission is disabled in the correctness/stress specs so the acquire p99<50ms budget is unaffected; override the sink with `MEGINGJORD_LOCK_TELEMETRY_FILE` or disable with `MEGINGJORD_LOCK_TELEMETRY_DISABLED=1`. Documents the cooperative-agent trust model + `0o600` limit.
