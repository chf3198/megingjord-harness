### Fixed

- **Evidence contradiction (Epic #3679):** a COLLABORATOR_HANDOFF that declares
  `cross_family_rating: UNAVAILABLE` (or N/A / none / a failing / REJECT verdict) alongside a
  cited `cross_family_receipt` is now a BLOCKING violation
  (`cross-family-rating-receipt-contradiction`) — no review means no receipt. The correct
  degraded path is a visible non-pass with NO receipt. (#3680)
