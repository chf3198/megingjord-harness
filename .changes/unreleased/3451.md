- **Per-feature × per-orchestrator parity matrix hard-gate (#3451, Epic #3411 T3.1):** `scripts/global/harness-parity-matrix.js` (`npm run parity:matrix`) live-probes every `parity:yes` (feature, runtime) cell against the catalog + reconciler — a declared-full cell whose ssotFiles are missing fails, a `structural-NA`/`waived` cell without a substituteTest fails, and a structurally-unreachable cell emits `make-reachable-required`. ssotFiles resolve across known source roots to avoid false positives. Ships **advisory-first** (`--strict` opt-in; advisory CI workflow `harness-parity-matrix.yml`); promotion to blocking is replay-eval-gated (T3.4 #3454). Stress spec asserts fault-injection (G6) + p99 budget (G7). Current real-repo scan: 490 ok / 95 advisory findings across 585 cells.
