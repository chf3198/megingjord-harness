### Fixed

- Worktree tool boundary now reaches the **Antigravity** runtime: the rule
  (file-editing tools only in the registered workspace; shell commands for sibling
  worktrees) is wired into `.antigravity/instructions.md`, the antigravity
  session-start adapter. Closes the residual gap from #3243, whose fix covered
  Claude Code (`CLAUDE.md`), Codex (`.codex/AGENTS.md`), and Copilot
  (`applyTo:**`) but missed antigravity, which does not auto-load `instructions/*.md`.
  Refs #3245 #3243 #3242.
