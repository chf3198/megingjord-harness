fix(governance): Wiki-B ticket mirror now refreshes on non-PR state changes (#3422, Epic #3398 C4)

The wiki-work-log-mirror workflow (Epic #3066) triggers on issue close/label/reopen but its bot commit ran the dev git hooks (validate-branch-name + the lefthook lint-router); the runner's missing `ruff` (lint-py) failed the commit, so Wiki B went stale on every non-PR state change (the #1899 casualty: mirror OPEN while live CLOSED/cancelled). The mirror commit + push now bypass those hooks (`--no-verify`), the stale #1899 mirror is backfilled to its terminal state as a regression fixture, and a golden-file test asserts the hook-bypass so the ruff-not-found class cannot silently return.
