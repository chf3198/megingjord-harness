- **Gate disposal path — the missing half of promotion** (#3811, Epic #3807 C3): new
  `scripts/global/gate-disposal-eval.js` gives the harness a symmetric *disposal* path for advisory
  validators (promotion had no counterpart, so 26/55 sat advisory indefinitely — full cost, no
  protection). The read-only evaluator assigns each advisory validator a `retain | promotion-candidate |
  retirement-candidate` disposition on positive evidence only (a reviewed redundancy finding, or
  below-floor replay precision after N windows); it flags, never deletes, and a blocking gate is never a
  candidate. Demonstrated by retiring `worktree-naming-advisory.js` — an advisory-only branch-name
  warner strictly dominated by the blocking `validate-branch-name.sh` + `branch-name.yml` gates —
  dropping the governance `surface_units` by 1 (advisory validators 26→25) with zero protective loss
  (the Epic's net-negative-surface invariant, cross-family verified). Runbook:
  `docs/howto/gate-disposal-path.md`.
