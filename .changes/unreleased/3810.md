fix(governance): make the free-fleet path the golden-path default for the raw-fleet-curl class (#3810)

Epic #3807 C2 (affordance-first). The #1 recurrence friction class
(`raw-fleet-curl-bypasses-hamr`, 1,615 lifetime incidents) fired because the correct path
was never the *default*. Added the memorable `npm run fleet -- --prompt "..."` golden-path
alias over the existing free-fleet `cascade-dispatch`, and rewrote the raw-fleet-curl DENY
redirect in `pretool_guard.py` to name that exact copy-pasteable command (poka-yoke). The
DENY, the bypass-incident, the `# hamr-bypass-ok:` diagnostic carve-out, and the
`MEGINGJORD_FLEET_DIRECT_BLOCK` / paid-provider controls are all preserved — only the friction
of doing the right thing is lowered. Census `surface_units` Δ = 0 (no counted governance unit
added; sensor retirement deferred to recurrence-decay).
