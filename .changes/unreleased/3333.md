- **Bounded fleet-failover in collaborator-preflight (#3333):** a dead/unreachable Tailscale fleet host no longer hangs cross-family review. `fleet-red-team-dispatch` gates the long generation behind a bounded reachability probe (reuses `fleet-probe`'s `/api/version` check, default 8s, `FLEET_PROBE_TIMEOUT_MS`); on probe failure it steps straight to the $0 free-cloud failover instead of blocking on the up-to-1500s generation patience. The preflight scoped runner also routes `@playwright/test` specs through `npx playwright test` so a playwright spec passed via `--test-paths` is executed, not reported as a false node:test miss.
