### Fixed

- **Family recognition (Epic #3679):** `signer-fidelity.js` `KNOWN_FAMILIES` is now derived from
  the cross-family receipt single source of truth (`PROVIDER_FAMILY` + `TEAM_FAMILY`), so the $0
  free-cloud panel families — `meta` (groq/llama, cerebras), `mistral`, `google` (gemini) — are
  recognized and a genuine `reviewer_family:` no longer trips a spurious `unknown-reviewer-family`
  advisory. (#3688)
