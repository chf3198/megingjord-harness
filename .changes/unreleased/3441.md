- **Catalog reconciler (#3441, Epic #3411 T1.3):** `scripts/global/harness-catalog-reconciler.js` derives per-runtime parity cells (`perRuntime.<rt>.status`) for every harness-feature-catalog feature from live committed inventory (runtime descriptors + governance manifest), a single derivation path that reconciles with the three legacy checkers (`state-store-parity`, `wiki-parity`, `orchestrator-governance-parity`) with zero conflicts. `npm run catalog:reconcile`. Fail-open (malformed inputs → `unverified`, never throws); p99 < 3ms over the full 223-feature catalog.
