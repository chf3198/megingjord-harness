- **Fleet tool-use reliability harness (#3487):** new `scripts/global/fleet-tool-use-harness.js` — grammar/JSON-schema-constrained tool-call dispatch with a bounded retry-on-malformed loop (schema threaded to the fleet, deterministic re-prompt on parse failure), a chain router that keeps only well-specified ≤2-call chains on the fleet (longer/underspecified escalate), and a measured reliability bar (`measureReliability`, default 0.9) that must be cleared before the fleet is trusted as the default tool-use reviewer. Scored against a labeled eval corpus (`tests/eval/fleet-tool-use-corpus.json`). Part of the Fleet Advisor capability (Epic #3414, §Q5).
