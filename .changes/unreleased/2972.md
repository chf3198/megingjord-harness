- fix(infra): make hook materialization cycle-safe so re-running `npm run hamr:activate` can never point a deployed hook at itself (the ELOOP "too many levels of symbolic links" that bricked every tool call and locked the operator out). `scripts/global/install-hooks.sh` now skips when a link is already correct, refuses a self-referential link, and cleanly replaces a stale/cyclic destination. `npm run hamr:doctor` now scans deployed hook scripts for cyclic/unreadable symlinks, reports any as an operator-lockout risk, and `npm run hamr:doctor --fix` removes the broken link so the operator can re-deploy. (#2972)
