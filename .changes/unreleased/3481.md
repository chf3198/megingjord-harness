- **Fleet Advisor Layer-② AI-research pass (#3481):** new `scripts/global/fleet-advisor-ai-pass.js` — the bounded, cross-family, $0 AI-research ceiling atop the deterministic lint floor. Composes a research prompt from the lint report, dispatches with bounded retry/backoff, and merges under a lint-authoritative policy (an AI-only finding is never auto-promoted to `high`). Trust controls: tiered freshness demotion (3d/7d/14d, dropped past 2×), citation-or-low-trust, and quantified-gain. Best-effort + non-blocking — degrades to lint-only (or a STALE-tagged cache) on panel outage. Part of the Fleet Advisor capability (Epic #3414).
