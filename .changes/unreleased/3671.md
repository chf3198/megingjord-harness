### Added
- Substrate-mixing guard in `scripts/global/megalint/signer-fidelity.js` (#3671): a runtime can no longer wear another team's `Team&Model`. Two registry-driven checks — `substrate-team-mismatch` (the substrate resolves via `substrateTeamMap` to a team that disagrees with the declared team) and `signer-seed-team-mismatch` (a `Signed-by` seed that is registry-unique to one team, e.g. `Cyrus`⇒`cursor`, paired with a non-matching `Team&Model`) — close the #1591 `codex:gpt-5` leak root cause. Regression suite `tests/cursor-signer-fidelity.spec.js` locks the #3087 Cursor registry-alias derivation across AC1–AC5.
