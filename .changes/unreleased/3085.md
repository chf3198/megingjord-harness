### Added
- Cursor 5th-runtime Phase 1 (#3085, Epic #3083): `.cursor/hooks.json` hook enforcement plus HAMR substrate parity. New `scripts/global/cursor-hooks-emit.js` is a pure camelCase↔PascalCase event adapter mapping Cursor's `sessionStart`/`beforeSubmitPrompt`/`preToolUse`/`beforeShellExecution`/`beforeMCPExecution`/`afterFileEdit`/`stop` plus the Cursor-native `subagentStart`/`subagentStop` onto the harness hook scripts; regenerate with `npm run cursor:hooks-emit`. `HAMR_TEAM=cursor` is now recognized by `hamr-activate.sh`, `hamr_activation_check.py` (sessionStart advisory), and `hamr-provider-wrapper.js`; `detect-runtime.js` detects the Cursor runtime; `hook-parity-check.js` tracks the `~/.cursor/hooks/scripts` deploy; and `deploy:cursor:apply` deploys hook scripts to `~/.cursor/hooks/`. The cross-runtime baton-artifact invariant test now covers cursor.
