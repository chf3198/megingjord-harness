feat(governance): epic-scaffold — $0 gh scaffold for compliant research-first Epics (#3713, Epic #3255 Phase-1)

Ships the single Phase-1 residual of Epic #3255 (per Phase-0 #3256): scripts/global/epic-scaffold.js + epic-scaffold-cli.js — pure builders + a thin gh CLI (dry-run default) that create a research-first Epic (correct labels + a just-in-time body with zero dev-ACs) and exactly ONE blocking R&P child by default, cross-team portable. It adds NO new rule; a round-trip composition self-check proves its own output passes the canonical validators (research-first-phase-gate.js + epic-ac-traceability.js) with zero violations, so the validators stay the single source of truth and the scaffold is a pre-satisfier. Hostile-title checkbox-AC injection is neutralized; apply throws rather than silently ship a body that fails the self-check.
