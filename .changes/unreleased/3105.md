### Added
- Test-floor calibration + audit layer (#3105, Epic #1948 Phase-1 P1.3–P1.6, the deferred enhancements on top of the #3098 classifier). New `scripts/global/test-floor-replay-eval.js`: `replayEval(corpus)` scores the classifier against a labeled corpus (`tests/fixtures/test-floor-corpus.json`) and gates the advisory→blocking promotion on precision ≥ 0.85 (`npm run test-floor:replay-eval`); `detectDrift(samples)` reports the under-declaration drift rate. The classifier gains `auditRecord(result, {ts})` (versioned `test-floor-audit-v1` schema) and a `TEST_FLOOR_DISABLED=1` rollback no-op. Building the corpus surfaced and fixed two real classifier bugs: nested `scripts/global/**/*.js` is now a governance-script surface, and python hooks are no longer flagged for missing the JS `stress-test` strategy. Cross-runtime parity, fault-injection, and p99 stress coverage added.
