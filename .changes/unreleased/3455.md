- **Harness hygiene: lease path + A4 isolation + phantom allowlist (#3455, Epic #3411 carve-out):** (A) standardized the cross-team lease store to the canonical `~/.megingjord/cross-team-leases.json` (was cwd-relative `.dashboard/`), with a `checkLegacyPath()` migration detector and backward-compat read; (B) fixed an A4 wiki-isolation leak — `deploy.sh` now `rsync --exclude='wisdom/project'` so project-scoped wisdom is never shipped cross-project (`tests/wiki-isolation-a4.spec.js`); (C) removed the phantom `vscode-extension` entry from the runtime side-effect ALLOWLIST (no such runtime) so an unknown runtime is correctly denied.
