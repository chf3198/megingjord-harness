### Added
- `npm run memory:read-route` (`scripts/global/read-router.js`, #3130): a tiered READ-router for the agent wiki plane (Epic #3124 D5). `routeQuery(queryClass)` returns the sub-wiki + retrieval strategy that loads the minimum context — symbol/signature→`wiki/code/symbols` (grep-index), ticket/pr→`wiki/work-log` (mirror-lookup), governance/wisdom→`wiki/wisdom` (index-first), heavy-sweep→sub-agent isolated 1-2K digest, unknown→index-first floor. Embeddings/graph are deferred (replay-eval-gated). `recallMiss()` emits a schema-v3 G8 signal so index-first degradation is observed, not silent. Deterministic, local, $0 — no embeddings.
