- **Fleet Advisor IT contract + /fleet-advisor skill + OpenAPI (#3482):** the capstone tying the Fleet Advisor together. `scripts/global/fleet-advisor-report.js` partitions merged findings into **Class A** (IT-actionable now, $0/reversible — runs via the it-ops bypass only with a durably-audited paired rollback; atomic-or-abort: no audit ⇒ no action; never files a ticket/commit), **Class B** (review → optional baton), and **Class C** (client hardware budget gate — never auto-spend), and renders an md+json report. A versioned `openapi/fleet-advisor.yaml` schema is validated cross-runtime (Claude/Codex/Copilot/Antigravity identical, G9), the AI prompt is hardware-only redacted (no host addresses/secrets), and the `/fleet-advisor` skill (`.claude/commands/fleet-advisor.md`) is the IT-role entry point. Completes Epic #3414.
