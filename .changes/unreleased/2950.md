### Fixed

- `hamr-sync-verify.js` now imports `verifyRuntimeParity` from `review-cli-parity.js`
  and runs the C9 parity check as part of `npm run hamr:sync-verify`. The check
  verifies all 6 review pipeline modules (`cascade-dispatch.js`, `fleet-backend-select.js`,
  `fleet-escalation-policy.js`, `review-stakes-router.js`, `review-cost-telemetry.js`,
  `review-privacy-gate.js`) are present and hash-matching in both `~/.copilot/scripts/`
  and `~/.codex/devenv-ops/scripts/`. Exit code 1 when any mismatch or absence is found.
- `npm run deploy` / `deploy:apply` now target `both` (Copilot **and** Codex) instead of
  Copilot only. Root cause of the Codex drift the C9 verifier surfaced: `deploy.sh`'s default
  target is `copilot`, so the canonical apply command never invoked the Codex deploy — Codex
  only updated under an explicit `--target codex|both|all`. `codex-runtime.js` was never at
  fault (it does a full recursive `cpSync` of `scripts/global`). CLAUDE.md's Deploy line is
  corrected to describe real per-runtime coverage.

Refs #2950 #2935 #2926
