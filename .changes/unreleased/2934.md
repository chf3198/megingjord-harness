### Added
- Review privacy gate (#2934, Epic #2926 C8): `scripts/global/review-privacy-gate.js` enforces D8 — the fleet (local Ollama) rung is the privacy-preserving $0 default and is never redacted (the diff never leaves the host); before ANY external (free-cloud/premium) dispatch the diff is run through `log-redaction.js`; and `MEGINGJORD_REVIEW_NO_EXTERNAL=1` (or a sensitive-repo flag) **skips free-cloud entirely**, allowing only fleet or DPoP/HAMR-contracted premium (free providers have no data-handling contract — a G4>G3 carve-out). A blocked sensitive free-cloud dispatch returns no payload, so external-dispatch-on-sensitive = 0. Reuses `log-redaction.js`.
