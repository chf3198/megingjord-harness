### Phase-0 hardening: grounded-expert review folded into the git-protocol synthesis (#3854)

Re-validated the Phase-0 git-protocol guardrail deliverable with 3 tool-equipped experts (web-RAG +
worktree access, median 90 PASS). Folded in their code-confirmed findings: fixed the GAP-B design (the
`actual>0` reject over-blocked routine 1–2-behind and reintroduced the #1771-banned absolute threshold →
now tier-gated recompute on `declared≠actual`); added **GAP-E** (detached-HEAD + branch-name-validation
bypass at `git worktree add`, folded into P1-a #3857); and applied wording/strengthening fixes (AC-R5
receipt is a derived digest; GAP-D sink is `.git/hooks`; GAP-C offline fallback + GAP-D G8 self-heal event
made explicit; strict:true scoped to low-concurrency; stash-global/worktree-abspath omissions noted).
Refreshed cross-family receipt on the corrected version.
