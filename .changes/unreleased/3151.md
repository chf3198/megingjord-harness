### Added
- Stress+benchmark suites backfilling the three surfaces shipped short this session (#3151, Epic #3147 S4): `tests/stress-governance-qa-cache.spec.js`, `tests/stress-read-router-recall.spec.js`, `tests/stress-compile-doc.spec.js`. Each runs **real multi-process concurrency** (20 parallel worker processes) and asserts a chaos/fault path (G6) + a p99 latency budget (G7) via the #3149 harness.

### Fixed
- The qa-cache stress suite **surfaced a real concurrency bug**: `governance-qa-cache` (#3142) loses updates under concurrent `put()` (13/20 survived — read-modify-write race). Tracked for a file-locking fix in #3155. (recallMiss confirmed concurrency-safe: 20/20 appends intact; compile-doc writes a complete entry, no torn write.)
