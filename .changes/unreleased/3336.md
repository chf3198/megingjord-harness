- **Committed merge-authority bridge to the effective mechanism (#3336, Option B):** instead of committing ineffective `permissions.allow` rules (a `permissions.allow` rule does not gate the Claude Code auto-mode classifier — #3342), #3336 lands a committed, test-guarded bridge that pins the scoped baton `autoMode` grant in `automode-provision.js` (Admin-merge-on-CONSULTANT_CLOSEOUT+green-CI, Consultant-close), plus a `feature-completion-governance` note declaring that bridge as the platform-recognized Admin-autonomous-merge / Consultant-autonomous-close authority and reaffirming the client is never a merge approver. Disposition ratified by 3/3 cross-model consensus.
