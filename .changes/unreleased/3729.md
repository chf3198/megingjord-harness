- **Wiki mirror automation cut over to a non-protected `wiki-mirror` branch (#3729, client-ratified Option B).**
  `wiki-reconcile-cron` + `wiki-work-log-mirror` now push mirror repairs to `wiki-mirror` with `--no-verify`
  (subsumes #3723) instead of `main` — the daily reconcile no longer GH013-fails on main's required-status-checks
  ruleset, and `main` branch protection is left completely untouched (no bypass actor, no stored credential).
  Fresh mirrors publish to `wiki-mirror`; the read-router/dashboard consumer cutover is a scoped fast-follow.
