- **Wiki Store A coverage + Store B consistency (#3300)**: raised code-wiki (Store A) coverage from 0.81 to **1.0** and cleared Store B's 2 consistency errors. Root cause for the unreachable 0.95: `scripts/wiki/drift-detector.js` credited coverage from `wiki/code/symbols/` only, so instruction sources (whose pages live in `wiki/code/concepts/`) were always counted uncovered — capping coverage at 0.9157. The detector now credits both sub-layers (symbols + concepts). Backfilled the 57 genuinely-missing code-wiki pages via `wiki:ingest:code`, and added Wiki B frontmatter to the two `wiki/work-log/tickets/3199-{design,acceptance}.md` detail docs. `npm run wiki:health` → both stores `ok`.
