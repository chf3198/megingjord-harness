- fix(scripts): worktree-governance-audit sources its valid sandbox-launcher set from the Epic #3411 runtime-catalog SSoT (`runtime-descriptor.listRuntimes()`) instead of a hard-coded `copilot|codex|claude-code` triple. `sandbox/antigravity` and `sandbox/cursor` now pass naming validation, clearing the false-positive baseline-drift red on `worktree-governance-required`. (#3642)
