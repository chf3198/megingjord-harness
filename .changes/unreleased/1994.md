### Changed
- `scripts/global/issue-transition.js` (hot-path script called on every baton transition) routes label-read/write through `github-dispatcher.js execute()` per the canonical pattern from #1995. New `set-labels` dispatcher operation handles PUT-labels API call with auto-discovered repo. Stdout format unchanged; `MEGINGJORD_MCP_DISABLED=1` preserves existing gh-CLI path. Refs #1994.

### Added
- `github-dispatcher.js` `set-labels` operation (atomic PUT-labels via `mcp__github__update_issue` MCP path or `gh api .../labels -X PUT` CLI path). Refs #1994.
