### Fixed
- **CI flake — `api-smoke-load` timing assertions** (#2703): converted the retry-recovery (AC5) and non-blocking-render (AC4) checks from wall-clock `toBeLessThan(<ms>)` budgets to deterministic Playwright route-interception assertions, so they no longer fail on slow GitHub-hosted runners (observed 4999ms on PR #2701 with no API change). The one legitimate latency metric (p95 cache-miss, AC2/AC3) is preserved but recalibrated to a CI-safe regression tripwire with documented rationale. Adds an in-file invariant separating behavioral from latency-metric assertions.
