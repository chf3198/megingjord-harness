### Added
- **Full-baton end-to-end test fixture** (`tests/baton-e2e.spec.js`, #2064): a deterministic,
  in-memory fixture that drives the complete Manager→Collaborator→Admin→Consultant cycle —
  building all four baton artifacts via the schema builder, passing each through its megalint
  validator, and driving the canonical baton FSM through `triage→ready→in-progress→testing→
  review→done`. Includes chaos/fault-injection paths (missing evidence bit, out-of-order
  transition, forged evidence, missing required field) and a p99 latency budget. Runnable via
  `npm run stress:baton-e2e`; auto-discovered by the CI test runner. No network dependency.
