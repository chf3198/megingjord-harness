### Added
- `scripts/global/fleet-hamr-weekly-report.js` (61 LoC) — aggregates `~/.megingjord/cache-stats.jsonl` (per #1339) into weekly markdown compliance report. `generateReport({statsPath, windowDays})` returns markdown. Reports fleet utilization ratio, per-provider counts, per-tier counts. Phase-1 child P1-5 of Epic #2150. Dashboard panel deferred to follow-on. 8 unit tests. Refs #2202.
