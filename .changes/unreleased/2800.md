### Added

- Fleet-dev observability + net cost-of-quality (#2800, P1-7 of Epic #2791): a CLI report
  (`npm run routing:fleet-cost-report`) + a dashboard panel ("🚀 Fleet Net Cost-of-Quality") that surface
  the fleet-development-share, the escalation-rate, and the NET cost-of-quality — gross generation saving
  MINUS verification overhead (cross-family critic/judge/mutation), i.e. the NET G3 gain, not gross — plus
  a human-audit surface (demotion patterns; deny-list/critic-drift placeholders pending #2798/#2797).
  Reads the #2795/#2796 telemetry with a bounded tail-read; does not touch the Copilot-owned dashboard
  cost files. New: `scripts/global/fleet-dev-cost-report.js`, `dashboard/api/fleet-cost-handlers.js`,
  `dashboard/js/fleet-cost-panel.js`, `/api/fleet-cost` endpoint.
