### Governance — per-review-point flaw-capture detection layer (#3429 + #3431 + #3432, Epic #3425)

The detection layer for programmatic flaw capture (batch lead #3429):

- **#3429 (P1-b)** — `scripts/global/review-point-checkpoint.js`: a per-review-point checkpoint that, at
  each baton-artifact build, reads the friction-candidate feed (`incidents.jsonl`) for the window and
  emits one checkpoint run-event (`surface=review-point:<role>`) so a missing checkpoint is itself
  detectable. Wired advisory into the `baton-artifact-builder` emit seam; `FLAW_CAPTURE_DISABLED=1` no-op.
- **#3431 (P1-d)** — `scripts/global/friction-sensors.js`: F2 retry-counter (≥N near-identical Bash/Edit
  invocations) and F5 self-correction (git revert/reset/`--amend`/stash/discard, edit-undo) detectors;
  emit via the existing `emitFriction` (#3165) schema; 3+ same-`pattern_id` low-sev escalate to medium
  via the existing `incidents.jsonl` recurrence count. No new schema/log.
- **#3432 (P1-e)** — `scripts/global/anneal-decision-backstop.js`: demotes the #1855 SessionEnd audit from
  the **primary** recognition check to the **backstop** — it now reconciles checkpoint run-events against
  the artifacts that should have disposed them (crashed/bypassed-baton detection). Same
  `anneal-decision-detector` core (one model, two firing points). Stays advisory.
