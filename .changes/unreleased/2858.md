### Fixed
- The canonical cross-family review CLI is now `cascade-dispatch.js` (#2858, Epic #2926 C1). `fleet-red-team-dispatch.js` is a library with no CLI entry point, so invoking it as a CLI exited *silently* — stranding the operator into a raw paid review call (the root cause of review bypassing the $0 fleet/free-cloud lanes). It now prints a redirect to `cascade-dispatch` and exits non-zero, and `skills/research-redteam-loop/SKILL.md` documents `cascade-dispatch` (`--prompt/--model`, fleet→free-cloud→escalation) as the canonical review entrypoint. Cross-family reviewed on the $0 free-cloud lane (gemini, ACCEPT 9/10).
