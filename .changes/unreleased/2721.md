### Added
- **Governance-chain registry + self-enforcing meta-validator** (#2721, Epic #2709): `config/governance-chains.yml` declares every governance chain's links with a `guarantee: enforced|auto-emitted` (rejecting `operator-discretionary`), and `scripts/global/megalint/chain-integrity.js` is a CI gate that enforces the meta-invariant -- no discretionary links, all enforcement points resolve, a touched governed-surface requires a registry delta, the registry self-protects, and existing enforcement cannot be silently weakened (policy-downgrade / "Signal C" detection).
