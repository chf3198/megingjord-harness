### Governance surface — retire redundant rubric-consistency validator (#3814, Epic #3807 C6)

Retired the standalone validator `scripts/global/megalint/consultant-rubric-consistency.js` by
inlining its rubric-floor-vs-verdict logic (`checkRubricVerdictConsistency`, `parseGScores`, `FLOOR`)
into its only consumer, the CI-wired `consultant-closeout.js`. The rubric-consistency property stays
enforced by the same gate; behaviour is byte-identical (5/5 spec preserved). Net governance-surface
change: **−1 validator** (census `surface_units` 155 → 154; cumulative Epic Δ −4 vs the committed
baseline of 158). No G1/G4 control weakened (cross-family merge-consensus receipt `31a29144fc4719f8`,
meta+mistral both PASS).
