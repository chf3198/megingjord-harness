### Added
- HAMR Cost Coverage dashboard panel (#1159, G3): new `/api/hamr-coverage` endpoint + `dashboard/js/hamr-panel.js` render the zero-cost-lane signals from `~/.megingjord/cache-stats.jsonl` — cache-hit-rate-7d gauge, per-provider call mix, **premium-share governor flag** (warns when premium share >20%/7d or cache-hit < SLO), and free/fleet/haiku/premium tier-mix. Single source of truth (`dashboard/api/hamr-coverage-handlers.js`); renders names/counts only (G4, no secret values); graceful empty/stale state (G6); WCAG-4.5:1 shape+text signaling and `prefers-reduced-motion` honored (a11y). De-conflicts with `quota-tracker`/`router-tracker` (consumes, does not duplicate). Operationalizes the cost-ascending mandate so G3 adherence is auditable (G8).
