fix(governance): deterministic linked-ticket resolution (#1614)

Add `scripts/global/linkage-resolver.js` — a single-source `resolveLinkedTicket(body)` that
replaces the first-match `/Refs\s+#(\d+)/i` scan which ignored the auto-close keyword and picked
the wrong issue when a non-ticket reference led the PR body. Precedence: `Closes`/`Fixes`/`Resolves`
→ `merge-evidence-deferred-final:` → first line-anchored `Refs #N`; `Refs Epic #N` is excluded.
Wired into `baton-gates.yml`, `closeout-lint.yml`, and `changelog-fragment-presence.js`. Adds
`tests/linkage-resolver.spec.js` + adversarial `tests/stress-linkage-resolver.spec.js`, and
`docs/howto/canonical-linkage-block.md`. Closes the long-recurring "Refs ordering fix" CI churn.
