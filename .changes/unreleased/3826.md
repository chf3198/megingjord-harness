- **Plan-rating promotion gate (Epic #3822 C2, Gap B):** a research-first Epic's Phase-0 can no longer be promoted past Phase-0 — nor can the Epic close — unless its Phase-0 plan carries a **verified cross-family rating receipt**. `phase0GreenComplete()` gains a `planRating.ok` conjunct fed by the new resolver helper `hasVerifiedPlanRatingReceipt()`, which is structural (reuses `cross-family-receipt.verifyReceipt`, kind:review — ≥2 distinct non-authoring PASS over the hash-chained ledger, catching the un-rated-plan class proven by the real #3808 miss) then semantic (committed `median ≥ 90`, `distinct_families ≥ 3`, `gwet_ac1 ≥ 0.6`). Enforced at `phase0-closure-guard` (un-rated close-block) and `phase1-auto-materialize` (won't seed an un-rated plan). Free-text `EPIC_RESCOPE`/`CONSULTANT_CLOSEOUT` remain necessary but are no longer sufficient. No new validator module; reuses the audited `PHASE0_GATE_BYPASS=1` hatch for in-flight un-rated Epics; governance-surface census net-negative.
