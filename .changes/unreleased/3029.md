### Added

- **`scripts/global/registry-version.js`** (#3029 C1 AC3): binds `registryVersion` hash to the signer registry signing surface; fail-closed on drift in `agent-signature.js` and `signer-registry-check.js`.
- **`scripts/global/megalint/registry-tuple-coverage.js`** (#3029 C1 AC2): CI check that every `autoModeCoverage` (team,model) tuple resolves to a team-specific alias, not a cross-team wildcard.
- **Copilot Auto-mode registry entries** in `inventory/team-model-signatures.json` for qwen, gemini, mistral, gpt-4/5, o-series models (#3020 class fix).

### Changed

- **`inventory/team-model-signatures.json`**: `autoModeCoverage` manifest + `registryVersion` field; explicit copilot entries before wildcards.
