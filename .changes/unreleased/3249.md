### Fixed

- `.github/workflows/danger.yml` — add `merge-bypass:admin-exception` label
  check with `continue-on-error` to demote persistent Danger.js
  `ERR_STREAM_PREMATURE_CLOSE` (Node 22 / GitHub API gzip compat bug) to
  non-blocking when admin has explicitly approved the exception (#3249)

Refs #3246
Closes #3249
