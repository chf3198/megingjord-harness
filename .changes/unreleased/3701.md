### Fixed

- **Bypass/closeout separation of duties (Epic #3679):** an excepted `--admin` bypass whose
  `approver:` resolves to the merging admin is now BLOCKING (`admin-bypass-self-approved`), and a
  `CONSULTANT_CLOSEOUT` whose signer equals the ADMIN_HANDOFF signer is BLOCKING
  (`consultant-admin-signer-collapse`) — one operator cannot be both admin and consultant. Broad
  closeout alias-fidelity ships advisory (blocking promotion replay-eval-gated). (#3701)
