### Added
- Advisory `epic-ac-disposition-check` megalint validator (#1617): flags an Epic that closes with an enforce-worded acceptance-criterion ticked while the shipped artifact is advisory-only (non-blocking), complementing `body-ac-truthfulness` (checkbox state) and `epic-ac-traceability` (child refs) on the wording-vs-shipped-disposition axis. Wired into the megalint index and a new advisory `epic-ac-disposition-lint.yml` workflow (`issues.closed`, `core.warning` only — never blocks; promotion replay-eval-gated). Epic Close Conditions gain an explicit condition that an `enforce X` AC must reflect the shipped artifact's disposition or be rescoped to `ship advisory; promotion deferred` before ticking.
