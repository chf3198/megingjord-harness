---
title: "#915 feat(hamr): core CF Worker /healthz /bundle /mcp #910"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [pr, wiki-b]
related: []
status: MERGED
source_path: "github:pr/915"
source_sha256: 29f429f75887a547a166d25a05c9f840db915c4a37704813dbd4ea64dbebd00f
content_hash: aaf4437553c85c8202db200e0dd4aa94b08f174e6fa8c9a936b3f6a2778c9d62
last_updated: "2026-06-17"
generated_by_run: local
---
# #915 — feat(hamr): core CF Worker /healthz /bundle /mcp #910

> **Source**: github:pr/915 | **merged: 2026-05-05T14:44:22Z** | **Labels**: none
> Mirror of `gh pr view 915` (derived; edit the GitHub item, not this page).

## Body

## Summary

HAMR Wave 2 child 1 per v3.2 §5 + v3.2.1 §R9.

**Production deployed live to `https://hamr.chf3198.workers.dev`** with R2 bucket `hamr-bundles` + KV namespace `HAMR_KV`. Operator-cost $0.

Refs #910
Refs #860

## Routes

| Route | Wave 2 status | Wave that finishes it |
|---|---|---|
| `GET /healthz` | ✅ live (tier-aware: tier1-full / tier2-degraded / tier3-offline) | unchanged through MVP |
| `GET /bundle/<profile>/<sha>` | ✅ live (R2-backed, KV edge-cache headers) | Wave 2 child 6 (#912) gates with SLSA |
| `POST /mcp` | ✅ live (DPoP gateway via Ed25519) — SLSA gate placeholder returning 503 | Wave 2 child 6 (#912) wires real `slsa-verifier` |
| `GET /mailbox/read` | 501 placeholder | Wave 3 child 5 |
| `POST /mailbox/write` | 501 placeholder | Wave 3 child 5 |
| `GET /quota` | 200 placeholder body | Wave 4 child 9 |

## Validation evidence

- `npx playwright test tests/hamr-worker.spec.js`: **10/10 passed** in 2.2 s
- `node scripts/lint-readability.js --max-warnings=420`: **PASS** (419 ≤ 420)
- `npx markdownlint-cli2 wiki/concepts/hamr-core-worker.md CHANGELOG.md`: 0 errors
- Live `/healthz` HTTP 200 verified post-deploy by `hamr-deploy.sh` R9.4 post-condition

## Architecture

- **Coexists with existing `cloudflare/worker.ts`** (megingjord-coord #740/#785) — HAMR ships as a NEW Worker. Coordinator stays in service until Wave 3 child 5 (mailbox) supersedes it. Preserves HAMR's "strict superset, never makes the harness worse" guarantee.
- **Routes split into `cloudflare/hamr/routes/*.ts`** — top-level `worker.ts` is 59 lines; longest route 49 lines.
- **`PUBLISHER_KEYRING`** is a wrangler secret (set via `wrangler secret put`), NOT committed.

## v3.2.1 R9 patterns applied

- **R9.1 Worktree-isolation**: deploy script tolerant of cwd.
- **R9.2 Cwd-vs-branch pre-flight**: refuses detached HEAD; pattern surfaced a real branch hazard during this PR (see Collaborator handoff).
- **R9.3 Sequential dispatch with backoff**: `/healthz` per-binding 1 s timeout.
- **R9.4 Idempotent tear-down**: HTTP-200 deploy + HTTP-404 teardown post-conditions.

## Operator-environment evidence

- `CLOUDFLARE_API_TOKEN` loaded from `.env` via shell export at deploy time; never logged or committed.
- R2 bucket `hamr-bundles` + KV namespace `HAMR_KV` (`a01abe088f454a59973e72736978b5e5`) created via Wrangler from operator's account.
- Net cost $0 (Workers-Paid + R2 10 GB free + KV included).

## Baton trail

- **MANAGER_HANDOFF**: posted on issue #910.
- **COLLABORATOR_HANDOFF**: posted on issue #910.
- **ADMIN_HANDOFF**: pending CI green + merge.
- **CONSULTANT_CLOSEOUT**: pending merge.

## Copilot Team boundary

No overlap with `dashboard/js/token-reconcile.js`, `scripts/global/token-*.js`, `cost-report.js`, `model-routing-engine.js`, or `instructions/role-baton-routing.instructions.md` v2.0 WIP. **Existing `cloudflare/worker.ts` (megingjord-coord) NOT modified.**

🤖 Generated with [Claude Code](https://claude.com/claude-code)
