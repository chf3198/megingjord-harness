---
title: "#2958 fix(scripts): deploy:apply targets Copilot and Codex"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [pr, wiki-b]
related: []
status: MERGED
source_path: "github:pr/2958"
source_sha256: 5deda82f2f34451deace49744785a430ddfa4e6089f878e783b84d37182c06ff
content_hash: adcc8a1e4d17d5ffe4fdfa3d49eec59e7c8b2d4b2c78747e3b0cb232d88e4eee
last_updated: "2026-06-17"
generated_by_run: local
---
# #2958 — fix(scripts): deploy:apply targets Copilot and Codex

> **Source**: github:pr/2958 | **merged: 2026-06-12T04:09:33Z** | **Labels**: none
> Mirror of `gh pr view 2958` (derived; edit the GitHub item, not this page).

## Body

Refs #2950

merge-evidence-deferred-final: #2950

Make the canonical `npm run deploy` / `deploy:apply` target `both` (Copilot + Codex) so the Codex review-pipeline modules stay in script parity by default. Root cause of the drift the C9 verifier surfaced: `scripts/deploy.sh` default target is `copilot`, so the canonical apply command never invoked the Codex deploy. `codex-runtime.js` was never at fault (full recursive cpSync). Completes the deploy-orchestration half of #2950 that PR #2956 (detection-only) left undone.

Evidence: `npm run deploy` dry-run now prints '── Codex Managed Scripts ──'; `npm run hamr:sync-verify` review_parity coverage=1.0, 12 checked, 0 mismatches. Cross-family review qwen2.5-coder:7b@fleet ACCEPT.

---

## MANAGER_HANDOFF

scope: Root cause: deploy.sh default TARGET is 'copilot', so the canonical npm run deploy:apply (called 'both runtimes' in CLAUDE.md) only ever deploys ~/.copilot — it never invokes the codex deploy (codex-runtime.js), which runs only under --target codex|both|all. That is why the C9 verifier saw the codex review modules stale despite repeated deploy:apply runs. codex-runtime.js itself is NOT at fault: it deploys scripts/global via a full recursive fs.cpSync tree copy (no narrow manifest). Fix: make the canonical deploy + deploy:apply npm scripts target 'both' (copilot+codex) and register both MCP targets, so codex stays in script parity by default; correct the CLAUDE.md Deploy line to describe real coverage; append the deploy-parity note to the existing .changes/unreleased/2950.md fragment. The C9 verifier is already wired into hamr:sync-verify by the prior slice (PR #2956) and now passes 12/12.
lane: lane:code-change
test_strategy: manual-verify
acceptance:
AC1: npm run deploy (dry) and deploy:apply now run codex-runtime.js deploy (Codex Managed Scripts appears in output) in addition to copilot. AC2: codex MCP target registered alongside copilot in both deploy and deploy:apply. AC3: deploy.sh remains unchanged; only package.json npm-script args change. AC4: CLAUDE.md Deploy line accurately states deploy:apply covers copilot+codex (script-mirror runtimes) and deploy:claude:apply covers Claude. AC5: npm run hamr:sync-verify shows review_parity coverage 1.0 / 12 checked / no mismatches for codex+copilot. AC6: .changes/unreleased/2950.md updated with the deploy-parity entry.

gates: lint-required, quality-required, baton-gates, pr-title-required, merge-evidence-pr-gate, docs-check-on-package-json (README regenerated if package.json scripts changed)
related_tickets: #2935 #2926 #2956
overlap_decision: no-overlap — edits package.json (deploy, deploy:apply scripts), CLAUDE.md (Deploy line), and the existing .changes/unreleased/2950.md fragment. deploy.sh itself is unchanged (it already supports --target both). No other in-flight worktree touches these. This is the deferred deploy-orchestration half of #2950; PR #2956 shipped only the detection half (hamr-sync-verify wiring).
goal_lens: G9 interoperability (cross-runtime review-pipeline parity) + G2 quality (root-cause the deploy orchestration, not the band-aid manifest hypothesis in the ticket body) + G6 resilience (codex no longer silently drifts between explicit codex-deploys). manual-verify: deploy orchestration is npm-script/config; before/after dry-run output + hamr:sync-verify parity are the objective evidence.

Signed-by: Orla Mason
Team&Model: claude-code:opus@local
Role: manager

---

## COLLABORATOR_HANDOFF

ticket: #2950

scope: Make the canonical npm run deploy / deploy:apply target 'both' (Copilot+Codex) so the Codex review-pipeline modules stay in script parity by default; correct the CLAUDE.md Deploy line; update the .changes/unreleased/2950.md fragment. deploy.sh unchanged.
test_strategy: manual-verify
per_ac_verification:
AC1 PASS: 'npm run deploy' (dry) now prints '── Codex Managed Scripts ──' / 'Would deploy: scripts' alongside the Copilot deploy. AC2 PASS: the resolved command runs 'xteam-mcp-register --target codex' after '--target copilot'. AC3 PASS: scripts/deploy.sh is not in the diff (git diff --cached shows only package.json, CLAUDE.md, .changes/unreleased/2950.md, README.md). AC4 PASS: CLAUDE.md Deploy line now states deploy:apply covers Copilot+Codex and deploy:claude:apply covers Claude. AC5 PASS: 'npm run hamr:sync-verify' review_parity = {parity:true, coverage:1, checked:12, mismatches:[], absentTargets:[]}. AC6 PASS: changelog fragment updated with the deploy-parity entry. README.md regenerated via 'npm run docs:compile' (package.json scripts changed).

doc_coverage:
UPDATED: README.md — regenerated by npm run docs:compile after the package.json deploy-scripts change. UPDATED: CLAUDE.md — Deploy line corrected. UPDATED: .changes/unreleased/2950.md — deploy-parity entry appended. N/A: vscode-extension/README — no extension/daemon behavior change (deploy orchestration only).

cross_family_rating: ACCEPT (qwen emitted no numeric; all three verification questions answered yes)
cross_family_reviewer: qwen2.5-coder:7b@fleet (<IPV4_REDACTED>:11434) — Alibaba family, cross-family vs claude-code:opus author
cross_family_findings: Q1 yes: --target both correctly fixes the 'codex never deployed by the canonical command' root cause. Q2 yes (no new risk): deploy:apply now also writes ~/.codex, which is the intended parity behavior; codex-runtime already did a full cpSync so no new write surface. Q3 yes: the CLAUDE.md correction accurately describes per-runtime coverage. Overall verdict ACCEPT. No defects surfaced.

Signed-by: Orla Harper
Team&Model: claude-code:opus@local
Role: collaborator

---

## ADMIN_HANDOFF

ticket: #2950

branch: fix/2950-codex-deploy-parity
commit: 73215212999d2367036ad09c25f63f79f5adf05e
signer-independence-check: PASS — Admin signer Orla Reyes differs from Collaborator signer Orla Harper (both registry-derived for claude-code:opus).
deploy-runtime-impact: The changed files (package.json deploy scripts, CLAUDE.md, README.md, .changes/unreleased/2950.md) are repo-only tooling/docs — none is itself a deployed runtime artifact, so no forward deploy of these files is required. The fix's EFFECT on deployed runtimes was verified: npm run hamr:sync-verify reports review_parity {parity:true, coverage:1, checked:12, mismatches:[], absentTargets:[]} for both Copilot and Codex. sync:codex / sync:claude (inverse runtime->checkout direction) are N/A here.

Signed-by: Orla Reyes
Team&Model: claude-code:opus@local
Role: admin

---

## CONSULTANT_CLOSEOUT

ticket: #2950

status: review
verdict: approve_for_merge
verification-timestamp: 2026-06-12T03:40:38Z
rubric_rating: 9/10 (G1:9 G2:9 G3:9 G4:9 G5:9 G6:9 G7:8 G8:9 G9:9 G10:9; min(G1..G10)>=7)
anneal_tickets_filed: none
mid_flight_flaws:
Flaw 1: the #2950 ticket body asserted the wrong root cause (a 'narrower/older manifest' in codex-runtime.js). Investigation disproved it — codex-runtime.js does a full recursive fs.cpSync of scripts/global; the real cause is deploy.sh's default target being copilot-only. decision=memory-note-only (correction captured in the MANAGER_HANDOFF + changelog; no separate ticket warranted). artifact=this CONSULTANT_CLOSEOUT. Flaw 2: #2950 was closed prematurely by PR #2956, which shipped only the detection half (hamr-sync-verify wiring) while the deploy-orchestration fix it also scoped was left undone. decision=no-action-justified (this reopen + fix IS the remediation; the deferred-finalize close discipline already exists). artifact=reopen of #2950 + this PR. Rubric: min(G1..G10) >= 7 — G9 interop restored (codex script parity by default, hamr:sync-verify 12/12), G2 quality (correct root cause), G6 resilience (no silent drift), G8 observability (the C9 gate from #2956 now backstops it). Cross-family review (qwen2.5-coder:7b@fleet) ACCEPT.
cross_family_verdict: ACCEPT — qwen2.5-coder:7b@<IPV4_REDACTED>:11434 — Q1/Q2/Q3 all yes; --target both correctly fixes the canonical-deploy root cause, introduces no new risk, and the CLAUDE.md correction is accurate.


Signed-by: Orla Vale
Team&Model: claude-code:opus@local
Role: consultant

