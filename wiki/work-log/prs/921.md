---
title: "#921 feat(mailbox): R2 JSONL + signed A2A envelopes #918"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [pr, wiki-b]
related: []
status: MERGED
source_path: "github:pr/921"
source_sha256: 9e63f2631795aa3c90433d1cc90bd2c69d4aff4d508e0825a3573f7b542c8398
content_hash: cc30c65796f54da5fda560a8e57f1a458d588cd7b23e04fec6e09673617e62f4
last_updated: "2026-06-17"
generated_by_run: local
---
# #921 — feat(mailbox): R2 JSONL + signed A2A envelopes #918

> **Source**: github:pr/921 | **merged: 2026-05-05T16:51:23Z** | **Labels**: none
> Mirror of `gh pr view 921` (derived; edit the GitHub item, not this page).

## Body

## Summary

HAMR Wave 3 child 5 per v3.2 §R2 + v3.2 §5 child 5 + v3.2.1 §R9. **REPLACES** Wave 2 #910 501 placeholders with production R2 JSONL mailbox + signed Google A2A envelopes + KV-backed replay protection.

**Production live** at `https://hamr.chf3198.workers.dev/mailbox/{read,write}` with end-to-end roundtrip verified (send 200 → poll 200 → replay 409).

Refs #918
Refs #860

## Routes

| Route | Method | Status |
|---|---|---|
| `/mailbox/write` | POST | 200 / 401 / 400 / 409 (replay_detected) |
| `/mailbox/read?recipient=<id>&since=<iso>` | GET | 200 with `{messages, next_since}` |

## Validation evidence

- `npx playwright test tests/mailbox.spec.js tests/baton-signing.spec.js`: **18/18 passed**
- Live roundtrip: send 200 → poll 200 returns envelope → replay 409 `replay_detected`
- `node scripts/lint-readability.js --max-warnings=420`: **PASS**
- `npx markdownlint-cli2 wiki/concepts/mailbox.md CHANGELOG.md`: 0 errors

## Architecture

- **A2A envelope**: `{headers: {nonce, expires_at, publisher_key_id, recipient_key_id}, body}`. Signature in HTTP header.
- **Replay protection**: `HAMR_KV.get('nonce:<uuidv7>')` check; on accept, sets nonce with TTL = `min(expires_at - now, 24h)`.
- **R2 storage**: `mailboxes/<recipient>/<yyyy-mm-dd>.jsonl`, append-only.
- **Stable operator key**: new `OPERATOR_KEY_SEED_B64` env override on `baton-signing.js` produces deterministic Ed25519 seed (T3-env tier).

## Files

- `cloudflare/hamr/routes/mailbox.ts` (REPLACES 501 placeholders)
- `cloudflare/hamr/worker.ts` (+1 line dispatch)
- `scripts/global/mailbox-client.js` + `mailbox-outbox.js` (new)
- `scripts/global/baton-signing.js` (T3-env env-seed override)
- `tests/mailbox.spec.js` (9 tests; live tests skip when seed unset)
- `wiki/concepts/mailbox.md`
- `package.json` scripts: `mailbox:send`, `mailbox:poll`, `mailbox:flush`
- `README.md` regenerated; `CHANGELOG.md` entry

## Operator-environment evidence

- `OPERATOR_KEY_SEED_B64` added to `.env`. Key ID `op-6d2106b44772cf4b`.
- `PUBLISHER_KEYRING` Worker secret set via stdin pipe (never echoed).
- `CLOUDFLARE_API_TOKEN` consumed via shell export from `.env`.
- Net cost $0 (R2 + KV reused from #910).

## Strict-superset preserved

`agent-coord-remote.js` (megingjord-coord Worker, #740/#785) NOT modified — continues to function unchanged. HAMR mailbox is the new path; migration is a follow-up.

## v3.2.1 R9 patterns

- R9.3 ≤3 s KV/R2 ops
- R9.4 idempotent on `(publisher_key_id, nonce)`; replay deterministic 409
- R9 failover via mailbox-outbox queue + flush
- R9.2 cwd-vs-branch pre-flight (explicit `--head feat/918-r2-mailbox`)

## Baton trail

- **MANAGER_HANDOFF**: posted on issue #918.
- **COLLABORATOR_HANDOFF**: posted on issue #918.
- **ADMIN_HANDOFF**: pending CI green + merge.
- **CONSULTANT_CLOSEOUT**: pending merge.

## Copilot Team boundary

No overlap with `dashboard/js/token-reconcile.js`, `scripts/global/token-*.js`, `cost-report.js`, `model-routing-engine.js`, or `instructions/role-baton-routing.instructions.md`. Existing `cloudflare/worker.ts` (megingjord-coord) NOT modified.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
