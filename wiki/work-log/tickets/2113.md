---
title: "#2113 Adopt main-checkout residue: gitignore-extend dist/ + planning/; commit analyze-governance-drift.js"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/2113"
source_sha256: 12600f1aca6919505f0c6eaecd2052b47f2722c6fa69033caf61947e3a51b9c2
content_hash: a379d94d2cca6ce246541aaa2c05b631550225e82189e7641b46c1f20dc5f001
last_updated: "2026-06-17"
generated_by_run: local
---
# #2113 — Adopt main-checkout residue: gitignore-extend dist/ + planning/; commit analyze-governance-drift.js

> **Source**: github:issue/2113 | **state: CLOSED** | **Labels**: type:task, status:done, priority:P3, area:scripts, area:governance, resolution:completed, lane:docs-research
> Mirror of `gh issue view 2113` (derived; edit the GitHub item, not this page).

## Body

## Problem

Four untracked files have accumulated in `/home/curtisfranks/devenv-ops/` (the main checkout) across multiple prior sessions of other teams:

- `dist/bundles/` — build output (`dist/` subtree; ~unknown size; tooling artifact)
- `planning/copilot-handoff-2026-05-16.md` — Copilot Team session handoff (6 days old)
- `planning/copilot-resume-prompt-2026-05-16.txt` — corresponding resume prompt
- `scripts/analyze-governance-drift.js` — governance-drift analysis script (legitimate, useful)

These files re-trigger `flags.code_touched=true` on every session-start, causing the stop-hook to repeatedly fire "Stop blocked: uncommitted changes; Admin baton incomplete." even after legitimate sessions ship cleanly via worktrees. This is the noise pattern that Epic #2091's Phase-1 children are designed to fix structurally; this ticket is a one-time adoption of the existing residue.

## Scope

Two complementary actions:

1. **Extend `.gitignore`** to cover `dist/` + `planning/` patterns. Both directories are intentionally local-area per `scripts/lint.js` IGNORE_PATHS (lint already treats `planning/` as untracked-local; this aligns git itself).
2. **Adopt `scripts/analyze-governance-drift.js`** into the tracked set. The script is a legitimate governance utility; should live in `scripts/` permanently.

## Acceptance Criteria

- [ ] AC1: `.gitignore` extended with `/dist/` (root-anchored, like `/tickets/`) and `/planning/` patterns. Anchored to root only so future per-Wiki `wiki/work-log/planning/` etc. would not be inadvertently ignored.
- [ ] AC2: `scripts/analyze-governance-drift.js` added to the tracked set. File preserved as-is from the current main checkout state.
- [ ] AC3: After merge + operator runs `git pull` on main, `git status` on `/home/curtisfranks/devenv-ops/` reports CLEAN (no untracked files attributable to this residue).
- [ ] AC4: Operator-visible documentation: brief note in CHANGELOG fragment explaining adoption + future-prevention via Epic #2091's Phase-1 children.

## Lane / test_strategy

`lane:docs-research` + `test_strategy: drift-lint`. Pure gitignore + file-add; no script behavior change.

## Phase relationship

Adjacent to Epic #2091 (state isolation root-cause fix). This ticket is a one-time cleanup of residue that pre-dates the structural fixes. Once Epic #2091's Phase-1 children #2103, #2106, #2107 are all merged + deployed, this kind of cross-team residue accumulation should not recur.

Refs Epic #2091

