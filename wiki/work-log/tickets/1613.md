---
title: "#1613 Self-anneal: implement real pre-push test-evidence diff validation"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/1613"
source_sha256: 2406eea71e62781dd7fd23f73e20bade2384807c186aa8e81b008e7db82dc871
content_hash: 01f2e61fb65e9b132d7d74010321b1401b50259ab5782489256dd3c688389405
last_updated: "2026-06-17"
generated_by_run: local
---
# #1613 — Self-anneal: implement real pre-push test-evidence diff validation

> **Source**: github:issue/1613 | **state: CLOSED** | **Labels**: type:task, status:done, priority:P2, area:governance, lane:code-change, governance:close-without-merge
> Mirror of `gh issue view 1613` (derived; edit the GitHub item, not this page).

## Body

## Parent / trigger

Review of Epic #1568 / #1570 found a concrete parity gap in the local pre-push gate.

`lefthook.yml` runs:

```sh
node scripts/global/test-evidence-validator.js --diff-only || true
```

But `scripts/global/test-evidence-validator.js` does not implement `--diff-only`; its CLI only reads `--strategy`, `--lane`, `--comments`, and `--pr-files`. The current command therefore falls back to `test_strategy=none` on `lane:code-change`, reports `none-disallowed`, then is suppressed by `|| true`.

## Goal

Make the pre-push test-evidence gate truthful: either implement a real local diff-only mode or remove the misleading command and document why the evidence gate cannot run locally yet.

## Acceptance Criteria

- [x] AC1 — Add a real `--diff-only` mode to `scripts/global/test-evidence-validator.js`, or replace the Lefthook command with a supported local mode.
- [x] AC2 — The local pre-push output must not report an expected validation failure that is immediately hidden by `|| true`.
- [x] AC3 — Update `tests/pre-push-gates.spec.js` and/or `tests/lefthook-pre-push.spec.js` to prove the configured command is supported.
- [x] AC4 — Update `docs/howto/pre-push-gates.md` to accurately distinguish blocking, advisory, and unavailable server-state gates.
- [x] AC5 — Confirm `npm run hooks:pre-push` exits cleanly in the intended baseline state without `SKIP_CLOSEOUT_PREFLIGHT=1` unless a real ticket-state violation exists.

## Harness goals

Primary: G1 Governance, G2 Quality, G7 Throughput, G8 Observability.

## Review evidence

- #1570 AC-2 specifically names `node scripts/global/test-evidence-validator.js --diff-only`.
- The merged validator does not parse that flag.
- The soft-fail hides the problem from local operators, which weakens the stated pre-push parity contract.

---
Signed-by: Quill Mason
Team&Model: codex:gpt-5.4@openai
Role: manager

