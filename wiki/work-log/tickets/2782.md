---
title: "#2782 pretool_guard fleet-bypass incident never written (missing module-level os import)"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/2782"
source_sha256: ce0d5768319123efbeb774dd8c1d7d19354ecfbd4ab1c6dfebb955591275903c
content_hash: 8969857f4ec401877f71a0b0d1a848eaeec4caf36c59f3000eebdec7c3fd7f6f
last_updated: "2026-06-17"
generated_by_run: local
---
# #2782 — pretool_guard fleet-bypass incident never written (missing module-level os import)

> **Source**: github:issue/2782 | **state: CLOSED** | **Labels**: type:bug, status:done, priority:P2, area:hooks, resolution:released, lane:code-change
> Mirror of `gh issue view 2782` (derived; edit the GitHub item, not this page).

## Body

## Problem
`hooks/scripts/pretool_guard.py` has **no module-level `import os`** (only a function-local `import os` at line 41, scoped to a different function). `_emit_fleet_bypass_incident` (line 122, shipped in #2739) calls `os.path.join`/`os.makedirs`/`os.path.expanduser` -> raises `NameError` -> swallowed by the function's own `try/except Exception: pass` (docstring: 'Never raises'). Net: the fleet-bypass **Tier-1 incident is never written** to `incidents.jsonl`. The security `ask` still fires; only the #2192-AC1 observability signal is dead (G8).

## Evidence
- `ruff check` -> F821 Undefined name `os` at pretool_guard.py:122-123 (baseline on main).
- Surfaced during #2770 lint:py. Incident: `~/.megingjord/incidents.jsonl` pattern_id `pretool-guard-missing-module-os-import-dead-incident`.

## Fix
Add module-level `import os` to pretool_guard.py; add a unittest exercising `_emit_fleet_bypass_incident` writes a line (the #2739 suite tested only `is_raw_fleet_curl`, so the dead path escaped). 

## Goal lens
G8 (observability — the designed incident signal is silently absent) + G2 (correctness). Root cause: an exception-swallowing helper masked a NameError; the test didn't exercise the emit path.
