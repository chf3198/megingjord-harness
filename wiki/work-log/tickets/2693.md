---
title: "#2693 Stale canonical-main baton-comment-build.js silently drops --fields-json (empty handoffs)"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/2693"
source_sha256: 370ba6f2adc02ff376d486d0aa9fc018607637eab13c34d0aa85816e9f00da58
content_hash: 02352a1b943ad7f9f1eaf87b6a9636d93c7962b9bd8f9ace4042167ba11e1f4c
last_updated: "2026-06-17"
generated_by_run: local
---
# #2693 — Stale canonical-main baton-comment-build.js silently drops --fields-json (empty handoffs)

> **Source**: github:issue/2693 | **state: CLOSED** | **Labels**: type:bug, status:done, priority:P2, area:scripts, lane:code-change, governance:close-without-merge
> Mirror of `gh issue view 2693` (derived; edit the GitHub item, not this page).

## Body

## Symptom

Three MANAGER_HANDOFF comments — on #2673, #2674, #2675 — were posted with NO
field content (just `## MANAGER_HANDOFF` + `ticket: #N` + signing footer),
despite being generated with a full `--fields-json` payload. Required schema
fields (`scope`, `lane`, `test_strategy`, `acceptance`, `gates`,
`related_tickets`, `overlap_decision`) were silently dropped.

## Root Cause

`--fields-json` delegation was merged to origin/main via #2671. The canonical
main checkout (`~/devenv-ops/`) is never pulled (policy #2107). Its stale
`baton-comment-build.js` treats `--fields-json` as an unknown flag → silently
ignored → falls through to legacy `buildBatonComment()` path → renders only
header + ticket + signing footer → exits 0 ("posted"). The operator sees no
error and the artifact is useless.

## G3 Alignment

- Missing `lane:` field causes routing fallback to paid providers (G3 direct
  violation: non-zero cost substituted for free lane).
- Each empty MANAGER_HANDOFF triggers at least one collaborator-gate CI
  failure + manual rerun cycle (wasted compute; G3 violation via compute cost).
- Fail-loud eliminates both: operator knows immediately, no CI rerun needed.

## Non-Goals

- Do NOT change the `--fields-json` structured-path behaviour (already correct).
- Do NOT change the legacy free-form path for non-MANAGER_HANDOFF artifacts.
- Do NOT add new required flags or break existing callers.

## Acceptance Criteria

- [ ] **AC1**: `baton-comment-build.js` exits 1 + stderr message on any
  `--<flag>` not in the recognized set:
  `['artifact','role','team-model','ticket','fields-json','summary',
  'related-tickets','overlap-decision']`.
  Error format: `error: unknown flag '--<name>'. Recognized flags: <list>\n`.
  Exit code must be 1, not throw/uncaught error.
- [ ] **AC2**: Legacy path for `MANAGER_HANDOFF` artifact: after
  `buildBatonComment()`, verify output contains all fields in
  `['scope','lane','test_strategy','acceptance','gates']`; if any absent,
  stderr `error: MANAGER_HANDOFF missing required field '<f>' in legacy
  output\n` + exit 1. Non-MANAGER_HANDOFF artifacts are unaffected
  (backward-compat preserved).
- [ ] **AC3**: `tests/baton-comment-build.test.js` (node:test + child_process):
  - T1: `--unknown-flag val` → exit 1 + stderr includes "unknown flag"
  - T2: multiple unknown flags (`--foo bar --baz`) → exit 1 for first unknown
  - T3: MANAGER_HANDOFF legacy path without `--summary` → exit 1 + stderr
    includes "missing required field"
  - T4: COLLABORATOR_HANDOFF legacy path without fields → exit 0 (no AC2
    restriction applies; backward-compat)
  - T5: `--fields-json` with valid JSON file → exit 0 + output contains
    `scope:` and `lane:` fields
  - T6: all recognized flags accepted → exit 0 (no false positives)
- [ ] **AC4**: `scripts/global/baton-comment-build.js` remains ≤ 100 lines
  after changes.

## Implementation Plan

### `scripts/global/baton-comment-build.js` (modify — AC1, AC2, AC4)

1. Add `const KNOWN_FLAGS = new Set([...])` constant after imports.
2. Add `function checkUnknownFlags()`: scan `process.argv.slice(2)` for tokens
   matching `/^--(.+)/`; if name not in `KNOWN_FLAGS`: stderr + `process.exit(1)`.
3. Call `checkUnknownFlags()` at top of `main()`, before flag reads.
4. Add `function checkLegacyOutput(artifact, output)`: for `MANAGER_HANDOFF`,
   regex-check output for each required field; missing → stderr + exit 1.
5. Call `checkLegacyOutput()` before `console.log()` in the legacy path.

### `tests/baton-comment-build.test.js` (new — AC3)

- `node:test` + `node:assert/strict` + `node:child_process` execSync
- 6 test cases per AC3. Isolated: each spawns the script as a subprocess.
- Uses `os.tmpdir()` + random filename for T5 temp JSON file; cleaned up in
  `after()`.

## Verification

```sh
npm run lint                                   # green (≤100 lines)
node tests/baton-comment-build.test.js         # 6/6 pass
node scripts/global/baton-comment-build.js \
  --unknown-flag test                          # exits 1; stderr: "unknown flag"
```

## Test Strategy

`tdd-pyramid`

Refs #2692

