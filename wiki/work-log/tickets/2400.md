---
title: "#2400 feat(governance): codify tier-graceful degradation pattern in harness-goals G5+G6"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/2400"
source_sha256: 6657d64fcc6c505590a7358dc68099f04f1484695d165a5aa73805306c38f308
content_hash: a474d3389879035c5ef2064c29189522ca374735fd3b894e90a0120e2f51ffb4
last_updated: "2026-06-17"
generated_by_run: local
---
# #2400 — feat(governance): codify tier-graceful degradation pattern in harness-goals G5+G6

> **Source**: github:issue/2400 | **state: CLOSED** | **Labels**: type:task, status:done, priority:P1, area:governance, lane:code-change, governance:close-without-merge
> Mirror of `gh issue view 2400` (derived; edit the GitHub item, not this page).

## Body

## Objective

Codify the **tier-graceful degradation pattern** in `instructions/harness-goals.instructions.md` G5+G6 paragraphs. Surfaced today during #1112 Phase-0 R&D synthesis when the client identified that the proposed plan failed G5 — the rule of thumb "use optimal resource when available, fall back to baseline when not" was implicit in operator practice but NOT codified in the canonical goals document.

Client direction: "Your plan can have a failover design that utilizes the optimal resource when available, but can fallback to the lowest level of basic availability if necessary. This rule of thumb and other harness development practices should be ingrained somewhere in the goals or governance."

This ticket scopes the immediate codification. The broader operator-memory-promotion audit is filed as backlog Epic #2399.

## Proposed amendment

Add an explicit "Tier-graceful degradation pattern" paragraph between G5 and G6 in `instructions/harness-goals.instructions.md`, plus a small clarifying sentence at the end of the G5 paragraph that points to the new pattern. The pattern text:

```
## Tier-graceful degradation (cross-cutting pattern between G5 and G6)

Every feature that can benefit from a higher-tier resource SHOULD use that
resource when available AND MUST degrade gracefully to the lowest available
tier when the higher resource is absent or unreachable.

- "Available" is determined by environment (G5): the operator's asserted
  MEGINGJORD_MINIMUM_TIER (Epic #2398) defines the highest tier the
  implementation may assume.
- "Unreachable" is determined by runtime (G6): network outage, rate-limit,
  authentication failure, or any other transient condition.
- The two are distinct: G5 absent means "this operator never has the resource";
  G6 unreachable means "the resource is normally present but currently down."
  Both lead to the same fallback path.

Reference implementation: scripts/global/mailbox-client.js MAY post to HAMR R2
when MEGINGJORD_HAMR_DISABLED is unset and the worker is reachable, falling
back to .gnap/messages/<team>/<timestamp>.json committed to the issue branch
when either condition fails. The fallback IS the default; the optimization
IS the upgrade.

Engineering practice: when introducing a feature that uses a tier-2-or-higher
resource, the same PR must ship the tier-1 fallback. Single-tier dependencies
on resources above the operator's asserted minimum are rejected at code review
as G5 violations.
```

Plus a one-sentence note at the end of the G5 paragraph: "See 'Tier-graceful degradation' below for the optimal-with-fallback pattern that bridges G5 and G6."

## Acceptance Criteria

- AC1: `instructions/harness-goals.instructions.md` updated with the tier-graceful-degradation paragraph between G5 and G6
- AC2: G5 paragraph extended with one-sentence cross-reference to the new pattern
- AC3: Test fixture in `tests/harness-goals-tier-graceful.spec.js` asserts the pattern text is present and the G5 cross-reference exists
- AC4: lint-line-cap (instructions are line-cap exempt per repo policy; but the file holds at < 200 lines)
- AC5: Cross-link from #2398 to this ticket noting the pattern is codified
- AC6: Cross-family fleet adversarial review verdict approve_for_merge

## Lane + signers

`lane:code-change` (instructions edit + new test), `priority:P1` (codifies an explicit G5 design rule), `area:governance`. test_strategy: `tdd-pyramid`.

Refs Epic #2398 · Refs #1112 #2397 (where the G5 gap surfaced)

Signed-by: Orla Mason
Team&Model: claude-code:opus-4-7@local
Role: manager

