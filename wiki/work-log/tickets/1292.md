---
title: "#1292 Build cross-issue dependency DAG with native API and text fallback"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/1292"
source_sha256: fd698b0675dbfa1236d65c98a209696838dff7085311947194f06625d87a2619
content_hash: 1629d84828488b74093b083472f7d173920ce5ebcb2977c52f0c3ca9a6f4b8df
last_updated: "2026-06-17"
generated_by_run: local
---
# #1292 — Build cross-issue dependency DAG with native API and text fallback

> **Source**: github:issue/1292 | **state: CLOSED** | **Labels**: type:task, status:backlog, status:done, priority:P2, area:scripts, lane:code-change
> Mirror of `gh issue view 1292` (derived; edit the GitHub item, not this page).

## Body

Refs #1271

## Scope

Build cross-issue dependency DAG `scripts/global/epic-dependency-dag.js`. All-issue scope (not Epic-only). Native GitHub issue dependency API + text-parse fallback. Cycle detection. Rate-limit budget (≤50 req/min cap; 80-min secondary-limit aware).

## Wave

Wave 3 (depth)

## Problem

Per Epic #1271 gap 4: "Cross-Epic AC dependencies create deadlocks". Synthesis decision §2 broadened scope from CC's Epic-only to all-issue per CP/CX consensus.

## Acceptance criteria

- [ ] `scripts/global/epic-dependency-dag.js` builds DAG from native GitHub dependency API (primary) + text parse `Depends-on:` `Blocks:` `Blocked-by:` `Coupled-with:` (fallback)
- [ ] Feature-detection: `gh api /repos/.../issues/N/dependencies` returns 404/403 → fall back to text parse without retry storm (R3 mitigation)
- [ ] Topological cycle detection; reports cycles with explicit issue chain
- [ ] `epic-close-readiness` extension: cycle-detection on Epic close attempt; blocks close on circular dependency
- [ ] All-issue scope (not Epic-only): all `type:*` issues participate in DAG
- [ ] Rate-limit budget: bulk-mode caps at 50 req/min with ETag cache (uses C9 rate-limit guard middleware)
- [ ] Permission checks: verify actor has repo access before querying dependencies
- [ ] Backoff strategy: 503 Unavailable triggers exponential backoff (1s → 2s → 4s → 8s)
- [ ] Test fixtures: linear deps (passes), cycle (blocks close), mixed native+text (reconciled), API unavailable (text fallback)

## Test strategy

`tdd-pyramid`

## Lane

`lane:code-change`

## Dependencies

C9 (rate-limit guard middleware) — strongly recommended; without it, this child must bring its own rate-limiter.

## Effort estimate

1.5d

## Risk

Medium (R3 native API may be plan-tier-gated) — mitigated by feature-detection + text fallback.

## Goal-lens justification

G1 Governance + G6 Resilience: prevents deadlock close-attempts.

## References

- Epic #1271 AC5
- CC R&D plan F5 (broadened to all-issue per synthesis)
- CX Fix E (native API + text fallback)
- CP cross-team review (broader DAG scope)
- GitHub REST API rate limits (80 content-creating req/min)

