---
title: "#2945 fix: extend canonical-main enforcer to block tracked-file edits (not just new file creation)"
type: work-log
content_trust_score: 0.6
created: "2026-06-17"
updated: "2026-06-17"
tags: [issue, wiki-b]
related: []
status: OPEN
source_path: "github:issue/2945"
source_sha256: 41af5a314bed50de892e8db802cb8feca9f609a1cae8c0b2ab935aa7b27605e7
content_hash: fb85ff03429b0d952cd1517da0697267dd90437cd2aeba7f3e4587e735e98dcd
last_updated: "2026-06-17"
generated_by_run: local
---
# #2945 — fix: extend canonical-main enforcer to block tracked-file edits (not just new file creation)

> **Source**: github:issue/2945 | **state: OPEN** | **Labels**: type:bug, priority:P1, area:hooks, role:admin, status:testing
> Mirror of `gh issue view 2945` (derived; edit the GitHub item, not this page).

## Body

## Problem

The `canonical_main_enforcer` in `hooks/scripts/pretool_guard.py` enforces the canonical-main read-only policy asymmetrically:

- `create_file` (new untracked files): **BLOCKED** correctly
- `replace_string_in_file` / `edit_file` (modifications to existing tracked files): **ALLOWED** — enforcement gap

Policy states: *Writes rejected: tracked files (the codebase)*  
Reality: only new untracked file creation is intercepted.

## Root Cause / Evidence

Detected in session `701dab5a` (2026-06-11, Copilot Auto model):

1. `worktree-session-start.sh copilot fix/2933-...` created a branch *in the canonical main checkout* (`/home/curtisfranks/devenv-ops`) rather than an isolated worktree directory.
2. Session edited two existing tracked files successfully (`pretool_guard.py`, `collaborator-preflight.js`) — enforcer did not intercept.
3. Session attempted to `create_file hamr_review_provider_block.py` — enforcer correctly blocked: "neither gitignored nor tracked".
4. Session stalled with no recovery path. Canonical-main left dirty with broken imports.

**Dirty state as of 2026-06-11:**
```
M hooks/scripts/pretool_guard.py         ← broken import (missing module)
M scripts/global/collaborator-preflight.js
```

## Acceptance Criteria

- [ ] AC1: `pretool_guard.py` intercepts `replace_string_in_file` / edit-type tool calls and applies the same canonical-main write policy check as `create_file`
- [ ] AC2: Attempting to edit a tracked file in canonical-main produces the redirect-to-worktree message: `canonical-main read-only (#2107): use a dedicated worktree`
- [ ] AC3: Gitignored files in canonical-main remain writable (per existing allowlist)
- [ ] AC4: Unit test: edit attempt on tracked file in canonical-main → deny decision emitted
- [ ] AC5: Unit test: edit attempt on gitignored file in canonical-main → allow decision emitted
- [ ] AC6: The dirty state from session `701dab5a` is reverted (`pretool_guard.py` + `collaborator-preflight.js` restored to HEAD)

## Anneal Context

- **Pattern ID**: `canonical-main-partial-write-allowed`  
- **Trigger**: Tier-2 auto-file (recurrence risk HIGH, same-class block will repeat in every Auto-mode session attempting new-file implementation work)
- **Session log**: `VSCODE_TARGET_SESSION_LOG = /home/curtisfranks/.config/Code/User/workspaceStorage/2af0f82e3aa12cdc78ff11f98545db6d/GitHub.copilot-chat/debug-logs/701dab5a-9dc4-4d2a-a7e2-3382c3773bb8`

## Fix Plan

1. In `pretool_guard.py`, add edit/replace tool names to the canonical-main check dispatch (alongside the existing `create_file` check)
2. Verify against the canonical-main allowlist: if the target path matches a gitignore pattern, allow; otherwise deny and emit redirect
3. Revert dirty canonical-main state as part of this fix branch

Refs #2933
