---
title: "#1580 feat: harden collaborator-self-check from heuristic patterns to schema-validated baton artifact checks"
type: work-log
content_trust_score: 0.6
created: "2026-09-13"
updated: "2026-09-13"
tags: [issue, wiki-b]
related: []
status: CLOSED
source_path: "github:issue/1580"
source_sha256: 8628aa288a4e03e7ea2d4b51322630c070b114105184f828575523f8a55b188a
content_hash: aef256b1f54dd43186c81aaa40634842f954fc4dae465d35446b333247168d55
last_updated: "2026-09-13"
generated_by_run: 34745647312
---
# #1580 — feat: harden collaborator-self-check from heuristic patterns to schema-validated baton artifact checks

> **Source**: github:issue/1580 | **state: CLOSED** | **Labels**: type:story, status:done, priority:P3, area:scripts, lane:code-change, governance:close-without-merge
> Mirror of `gh issue view 1580` (derived; edit the GitHub item, not this page).

## Body

## Context

Opened as follow-up from CONSULTANT_CLOSEOUT on #1571 (merged PR #1579).

The collaborator-self-check rules (G5/G6/G9 gap) currently use regex heuristics to inspect baton artifact text (e.g., `COLLABORATOR_HANDOFF` body pattern matching for AC ticks, flaw-marker citations, etc.). This is functional but brittle compared to schema-validated structured fields.

## Problem

- `allAcceptanceCriteriaTicked` — counts `[x]` checkboxes in the handoff body; fails silently if the AC section is absent or uses a non-standard format
- `flawMarkerCitations` — regex scan within ±2 lines; not tied to the baton schema contract
- `noProseColonCollision` / `noMarkdownBoldOnTestStrategy` — text pattern checks that could produce false positives on legitimate prose

## Acceptance Criteria

- [ ] AC-1: Define a machine-readable schema for COLLABORATOR_HANDOFF structured fields (AC table, pre-handoff section, test-strategy value)
- [ ] AC-2: Update `collaborator-self-check-rules.js` to validate against schema fields rather than raw text patterns where a schema field is available
- [ ] AC-3: Maintain full backward compatibility — waiver label `collaborator-self-check:waived` must still silence all checks
- [ ] AC-4: 15+ tests; all existing tests must continue to pass

## Notes

- Low urgency (P3); current heuristics are passing and catching real issues
- Do not regress the test baseline from #1571 (now 25/25)
- Origin: #1571 (closed). Formerly tracked under Epic #1568, which is now **closed/done** — this is an **independent backlog ticket** with no active parent. (Parent-prose normalized 2026-06-29 per drift audit; the prior `Epic #1568` prose form evaded native parent-detection.)

---

> **Manager grooming note (2026-06-29, drift remediation):** Scope reconciled against current code state.
> Since this ticket was filed (2026-05-15), structured-field hardening for COLLABORATOR_HANDOFF
> partially landed in `scripts/global/megalint/collaborator-handoff.js` via independent tickets —
> line-anchored `Role:` field (#2921), 16-hex `cross_family_receipt` format (#2904), and the
> self-check hard-gate promotion (#2907). That work is a **second, divergent regex parser**; AC-1
> (a single machine-readable schema source-of-truth) remains genuinely unmet. AC-2 is read against
> its own "where a schema field is available" qualifier: structured fields are schema-validated;
> inherently free-text prose checks (flaw-marker proximity, AC-checkbox counting) remain documented
> heuristics. See MANAGER_HANDOFF comment for the reconciled deliverable.

Signed-by: Soren Vale
Team&Model: copilot:claude-sonnet-4-6@github-copilot

