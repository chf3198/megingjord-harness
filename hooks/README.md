# Hooks Directory

Git hooks and deployment automation scripts.

## Planned Hooks

- **pre-deploy**: Validate skill format before deploying
- **post-merge**: Auto-deploy skills after merge to main
- **pre-commit**: Lint check on all files

## Installation

Hooks are installed to `.git/hooks/` via:

```bash
# Future: npm run install:hooks
```
